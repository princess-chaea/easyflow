# 이지플로우(EasyFlow) 백엔드 연동 가이드

이 문서는 현재 순수 정적 HTML/Tailwind 프로토타입(빌드 시스템 없음, 백엔드 없음, 데이터는 전부 localStorage 또는
하드코딩된 더미)으로 만들어진 프론트엔드를 실제 백엔드에 연결하기 위한 참고 자료입니다. 백엔드 담당자가 이 문서만
보고도 "지금 무엇이 진짜 동작하고 무엇이 가짜인지", "어떤 데이터를 어떤 모양으로 저장해야 하는지"를 파악할 수
있도록 정리했습니다.

작성 기준: 2026-08-05. 프론트엔드 전체 39개 페이지(관리자 11 + 업무배송 4 + 서식자료실 6 + 인생도서관 5 +
안내센터 3 + 공통 인증/법적고지 8 + 홈 1 + 챗봇 1) 및 `assets/js/` 공유 모듈 6개를 전수 조사했습니다.

---

## 0. 가장 먼저 읽을 것 — 실제로 동작하는 것 vs 전부 장식인 것

프로토타입 특성상 "그럴듯해 보이지만 버튼을 눌러도 아무 일도 안 일어나는" 화면이 대부분입니다. 아래는 **실제로
localStorage를 읽고 쓰며 상태가 바뀌는, 진짜 동작하는 흐름**의 전체 목록입니다. 이 목록에 없는 모든
버튼/업로드/차트/AI 응답은 정적 텍스트이거나 `setTimeout` 연출입니다.

| 기능 | 관련 파일 | 저장 키 |
|---|---|---|
| 로그인/로그아웃/역할 표시 | 공통_로그인.html, assets/js/auth.js | `currentUser`(문자열), `currentRole` |
| 역할 추가 신청 → 승인 | 공통_회원가입.html, profile-modal.js, 서버관리자_역할관리.html | `roleRequests`(배열), `grantedRoles_<email>` |
| 1:1 문의 등록/답변 | 안내센터_1대1문의관리.html, assets/js/inquiries.js | `ef_inquiries` |
| 멘토링 신청/답변 | 인생도서관_마이페이지.html, 인생도서관_멘토상담함.html, assets/js/mentoring.js | `ef_mentoring` |
| 업무 체크리스트 점검 | 인생도서관_업무체크리스트.html | `ef_checklist_progress_<org>` |
| 공지사항 작성(서버관리자) | 안내센터_공지사항.html | `ef_notices` |
| 홈 위젯 배치 | index.html | `ef_home_widgets_<email>` |
| 프로필 아바타 업로드 | profile-modal.js | `avatar_<email>` |
| 푸터/연락처/DPO 정보 편집 | 서버관리자_푸터관리.html | `ef_footer_*`, `ef_contact_*`, `ef_dpo_*` |
| 업무발송 → 업무우편함 | 업무배송_업무발송.html, 업무배송_업무우편함.html | `ef_mail_data` (단, **1.1의 버그로 실제로는 거의 항상 실패**) |
| 업무우편함 구독/책갈피 | 업무배송_업무우편함.html | `ef_mailSubscriptions_<user>`, `ef_mailCalendarBookmarks_<user>` |
| 학사캘린더 개인 일정/완료 체크 | 업무배송_스마트공문달력.html | `ef_events`, `ef_done`, `ef_settings` 등 |
| NEIS 학사일정 조회 | 업무배송_스마트공문달력.html, school-search.js | 외부 API(NEIS Open API), 실시간 |

나머지(서버관리자 대시보드/RAG관리/리소스모니터링/보안설정, 학교관리자 대시보드/RAG갱신/리포트/학교통합관리,
서식자료실 4종, 인생도서관 통합대시보드, 업무배송_맞춤형업무가이드 등)는 **전부 정적 데이터이거나 UI만 있고
로직이 없는 화면**입니다. 섹션 4에 페이지별로 상세히 정리했습니다.

---

## 1. 백엔드 설계 전에 반드시 알아야 할 크로스커팅 이슈

아래 항목들은 특정 페이지 하나의 문제가 아니라 **여러 페이지가 공유하는 데이터 모델/컨벤션이 서로 어긋나 있는
문제**입니다. 백엔드 스키마를 페이지별로 그대로 베끼면 이 불일치까지 같이 이식되므로, 먼저 어느 쪽을
"정답"으로 통일할지 결정하고 시작하는 것을 권장합니다.

### 1.1 `currentUser`가 파일마다 다르게 취급됨 — 업무발송이 사실상 항상 실패

- `공통_로그인.html`과 `assets/js/auth.js`는 `currentUser`를 **순수 문자열(이메일)**로 저장/사용합니다.
  (`localStorage.setItem('currentUser', email)`)
- 그런데 `업무배송_업무발송.html`(제출 로직)과 `assets/js/member-management.js`(서버관리자_회원관리.html)는
  `JSON.parse(localStorage.getItem('currentUser'))`로 **JSON으로 파싱을 시도**합니다.
- 이메일 문자열은 유효한 JSON이 아니므로 `JSON.parse("hong@gbe.kr")`는 예외를 던집니다. try/catch가 없어서
  **함수 전체가 조용히 중단**됩니다 — 사용자에게는 에러도 성공 메시지도 뜨지 않고, 버튼이 반응 없는 것처럼
  보입니다.
- 실질적 영향: **업무배송 담당자가 업무발송을 제출해도 `ef_mail_data`에 아무것도 추가되지 않습니다.**
  이것이 이전부터 알려진 "업무발송 → 업무우편함이 연결 안 됨" 문제의 정확한 원인입니다(설계는 같은 키를
  공유하도록 되어 있었으나 이 버그로 실행 전에 죽습니다).
- 추가로 `업무배송_업무발송.html`은 `currentUser.dept`(부서) 필드도 참조하는데, 이 필드는 회원가입 폼에
  애초에 존재하지 않습니다 — 버그를 고쳐도 발신 부서는 항상 `'해당부서'`로 고정 표시됩니다.
- **백엔드 조치**: 실제 인증을 세션/JWT로 교체하면서 "현재 로그인 사용자" 정보를 서버가 내려주는 구조로
  바뀌면 이 문제는 자연히 해소됩니다. 다만 프론트가 이 값을 참조하는 모든 지점(위 두 파일 포함)을 새 인증
  방식에 맞게 같이 고쳐야 합니다.

### 1.2 `roleRequests`가 두 가지 다른 모양으로 쓰이고 있음

- `공통_회원가입.html`, `profile-modal.js`, `서버관리자_역할관리.html` — **배열**로 사용:
  `[{email, requestedRole}, ...]`
- `assets/js/member-management.js`(서버관리자_회원관리.html) — **객체**로 사용:
  `{ "email@x.com": ["role1", "role2"], ... }`
- 결과: 회원가입에서 실제로 신청이 들어와도, 회원관리 화면의 승인 UI는 이 데이터를 절대 읽지 못합니다
  (`roleRequests[user.email]`가 항상 `undefined`).
- **백엔드 조치**: `role_requests` 테이블(id, user_id, requested_role, status, created_at)로 정규화하고,
  두 화면 모두 이 테이블 하나만 보도록 통일. 프론트 두 곳의 로컬 로직은 모두 폐기.

### 1.3 `grantedRoles` 키 규칙도 두 가지가 공존

- `auth.js`(실제 권한 판정에 사용), `profile-modal.js`, `서버관리자_역할관리.html` — 사용자별 키:
  `grantedRoles_<email>` (JSON 배열)
- `assets/js/member-management.js` — **전역** 키 `grantedRoles` (이메일 구분 없음)
- 결과: `서버관리자_회원관리.html`에서 "승인" 버튼을 눌러도 실제 권한 판정 로직(`EF_ROLE.effective()`)이
  보는 저장소에는 반영되지 않습니다. **역할관리.html이 실제로 동작하는 승인 화면이고, 회원관리.html의 승인
  UI는 눈속임에 가깝습니다.**
- **백엔드 조치**: 1.2와 함께 `role_requests`/`user_roles` 테이블로 통합. 서버관리자_회원관리.html과
  역할관리.html이 같은 데이터를 다른 뷰로 보여주는 형태로 재설계 필요.

### 1.4 서버관리자_회원관리.html은 회원관리.html·역할관리.html 둘 다와 사실상 무관한 별도 목업

- 회원관리.html이 표시하는 회원 목록(`mockUsers`, 5명, `@test.com` 가짜 계정)은 실제 가입자 데이터와
  전혀 연결되어 있지 않습니다. 헤더의 "전체 회원 1,248명" 같은 통계도 고정 텍스트입니다.
- **백엔드 조치**: `users` 테이블이 생기면 이 페이지는 그 테이블을 페이지네이션 조회하는 화면으로
  완전히 새로 구현해야 합니다. 현재 프론트 코드는 참고용으로도 거의 재사용 가치가 없습니다(스키마가 실제
  요구사항과 다름).

### 1.5 권한 검사가 전적으로 클라이언트 사이드

- `assets/js/auth.js`의 `PAGE_ROLE_REQUIREMENTS`/`ROLE_GATED_LINKS`가 페이지 접근·메뉴 노출을 제어하지만,
  이건 브라우저 devtools에서 `localStorage.setItem('currentRole', '서버관리자')` 한 줄이면 누구나 우회
  가능합니다.
- `assets/js/inquiries.js`, `assets/js/mentoring.js`의 `add()`/`answer()`/`respond()` 같은 데이터 함수
  자체에도 호출자 권한 검사가 전혀 없습니다 — 콘솔에서 아무나 `EF_INQUIRY.answer(1, '아무거나')` 호출 가능.
- 정적 파일 서빙 특성상 **서버가 렌더링에 관여하지 않으므로, 관리자 페이지의 HTML/JS 자체는 이미 누구에게나
  전달됩니다.** 데이터가 실제 API로 옮겨가면 이 문제는 훨씬 심각해집니다(단순 UI 숨김이 아니라 진짜 데이터
  유출/변조 가능).
- **백엔드 조치**: 모든 API 엔드포인트는 프론트의 역할 문자열을 신뢰하지 말고, 서버 세션에 저장된 역할로
  자체 인가(authorization)를 수행해야 합니다. `PAGE_ROLE_REQUIREMENTS`의 매핑표는 "어떤 페이지에 어떤
  역할이 필요한가"에 대한 **요구사항 명세**로만 재사용하고, 실제 검사는 서버에서 새로 구현.

### 1.6 외부 API 키가 프론트 소스에 그대로 노출됨

- `업무배송_스마트공문달력.html`: NEIS Open API 키가 하드코딩 (`NEIS.KEY = 'de0d40e40d9f4c9b954b0e0ca1161bfa'`).
- `assets/js/school-search.js`: 회원가입/프로필의 학교 검색에 사용하는 NEIS API 키도 하드코딩
  (`04f275416e194b508bbd3ad51e42d887`) — 위와 다른 키. 일일 호출 제한이 있는 키라 소스보기만으로 누구나
  확인/소진 가능.
- `업무배송_스마트공문달력.html`의 Gemini AI 연동부는 현재 키가 플레이스홀더(`YOUR_API_KEY_HERE`)이지만,
  구조상 실키를 넣으면 그대로 클라이언트에 노출되는 방식입니다. 코드 내 주석에 개발자가 이미 "운영 배포 시
  서버 프록시로 이전 필요"라고 인지하고 남겨둔 상태입니다.
- **백엔드 조치**: NEIS 조회와 Gemini(또는 다른 LLM) 호출 모두 **서버 프록시 엔드포인트**로 이전
  (`GET /api/neis/schools?query=...`, `POST /api/ai/draft` 등). 클라이언트에는 어떤 외부 API 키도 남기지
  않음.

### 1.7 XSS: 업무우편함이 이스케이프 없이 innerHTML 사용

- `업무배송_업무우편함.html`의 `renderMailList()`는 메일의 `title/desc/file/dept/category` 값을 이스케이프
  없이 문자열 결합으로 `innerHTML`에 삽입합니다(속성값에도 따옴표 이스케이프 없음).
- 이 값들은 `업무배송_업무발송.html`에서 **업무배송 담당자가 직접 입력하는 제목/설명/업로드 파일명**이므로,
  악의적인(혹은 실수로 HTML 특수문자가 들어간) 제목이 저장형 XSS로 이어질 수 있습니다.
- 참고로 같은 세션에서 이미 고친 사례가 있습니다: `AI업무어시스턴트_챗봇.html`은 원래 사용자가 입력한
  채팅 텍스트를 그대로 `innerHTML`에 꽂고 있었는데, `escapeHtml()` 헬퍼를 추가해 수정했습니다
  (`div.textContent = str; return div.innerHTML;` 패턴 — 다른 곳에도 그대로 재사용 가능).
- **백엔드 조치**: 이건 사실 프론트만 고치면 되는 문제이지만(서버가 직접 원인은 아님), API가 반환하는
  텍스트 필드를 프론트가 항상 이스케이프 후 렌더링하도록 코드 리뷰 시 체크리스트에 포함시킬 것을 권장.
  서버 쪽에서도 저장 시 악성 스크립트 패턴을 막는 입력 검증은 방어선으로 추가하는 게 안전합니다.

### 1.8 신규 가입자는 영구히 로그인할 수 없음

- `공통_회원가입.html`은 제출 시 `roleRequests`에만 기록하고, **계정 자체(이메일/비밀번호)는 어디에도
  저장하지 않습니다.**
- `공통_로그인.html`은 하드코딩된 6개 테스트 계정(`test@gbe.kr` ~ `test5@gbe.kr`, 공용 비밀번호
  `test1234!`)만 인식합니다.
- 이메일 인증 PIN도 가짜입니다 — `공통_회원가입.html`의 PIN 검증은 **6자리 숫자이기만 하면 무엇이든 통과**
  시킵니다(실제 발송된 코드와 대조하지 않음).
- **백엔드 조치**: `POST /auth/signup`(계정 생성 + 비밀번호 해싱), `POST /auth/send-verification` +
  `POST /auth/verify-pin`(실제 발송/대조), `POST /auth/login`(세션/JWT 발급)이 모두 새로 필요합니다.
  로그인 페이지의 하드코딩 6계정과 "[개발용] 빠른 역할 선택" 드롭다운(코드 주석에 이미 "추후 제거" 명시)은
  실배포 전 반드시 제거.

### 1.9 프로필 수정(소속·직위)이 저장되지 않는 버그

- `profile-modal.js`가 주입하는 프로필 모달에서 소속/직위 입력란은 있지만, "확인 및 닫기" 버튼
  핸들러가 모달을 닫기만 할 뿐 `org_<email>`/`rank_<email>`에 값을 저장하는 로직이 없습니다. 아바타
  업로드만 즉시 저장됩니다.
- 참고로 `공통_프로필수정.html` 파일 자체는 **도달 불가능한 죽은 페이지**입니다 — `profile-modal.js`가
  페이지 로드 시 이 링크와 설정(톱니바퀴) 아이콘을 DOM에서 제거하고, "프로필 설정" 클릭을 가로채 자체
  모달을 띄웁니다. 실제 프로필 편집 화면의 스펙은 이 HTML 파일이 아니라 `profile-modal.js`가 주입하는
  모달 쪽을 기준으로 삼아야 합니다.
- **백엔드 조치**: `PATCH /users/me {org, rank}` 필요. 프론트 저장 누락 버그는 백엔드 연동 시 같이 수정.

---

## 2. 제안하는 백엔드 아키텍처 개요

### 2.1 인증
- 세션 쿠키 또는 JWT 중 택1. 현재 `currentUser`/`currentRole`을 그대로 대체.
- 역할(교직원/장학사/업무배송 담당자/멘토/학교관리자/서버관리자) — 정확히는 base role(가입 시 자동
  "교직원") + 승인받은 추가 역할(복수 가능, `grantedRoles`)의 조합. `auth.js`의 `EF_ROLE.effective()`
  로직(base + '교직원' + granted[])을 서버 인가 로직으로 그대로 이식 가능.
- 서버관리자는 전 페이지 접근 가능(현재 `EF_ROLE.is('서버관리자', ...)`로 하드코딩된 최상위 규칙) — 이
  규칙은 유지 권장.

### 2.2 핵심 테이블 (요약)

localStorage 키 분석을 기반으로 도출한 최소 테이블 목록입니다. 정확한 컬럼은 섹션 4/6 참고.

- `users` (id, email, password_hash, name, org, dept, rank, avatar_url, created_at)
- `role_requests` (id, user_id, requested_role, status, created_at)
- `user_roles` (user_id, role) — 승인된 역할 (grantedRoles 대체)
- `inquiries` (id, user_id, type, title, content, status, answer, answered_by, answered_at, created_at)
- `mentoring_requests` (id, requester_id, category, title, content, status, answer, answered_by, answered_at, created_at)
- `checklist_progress` (org, period, sector, category_id, item_no, result, action, checked_by, checked_at) —
  또는 `checklist_responses` 단일 행 테이블로 정규화 권장(현재는 org당 통째로 JSON 하나)
- `notices` (id, badge, title, date, views, author, body, created_by, created_at)
- `dispatches` (id, sender_id, title, desc, categories[], due_date, dispatch_timing, status, created_at)
- `dispatch_files` (dispatch_id, filename, url) — 현재 프론트는 다중 파일 중 1개만 저장하는 버그가 있음(3.3 참고)
- `mail_subscriptions` (user_id, category)
- `mail_bookmarks` (user_id, mail_id)
- `mail_reads` (user_id, mail_id, read_at) — 현재는 메일 자체의 전역 속성처럼 처리되어 사용자별 읽음 상태가 없음
- `personal_events` (id, user_id, title, date, memo) — 스마트공문달력의 개인 일정
- `home_widget_layouts` (user_id, widget_id, size, sort_order) — 홈 위젯 배치
- `saved_resources` (id, user_id, filename, size, saved_at) — 마이페이지 "저장한 자료" (실제 파일 스토리지 필요)
- `site_settings` (key, value) — 푸터 기관명/주소/저작권, 개인정보 담당부서/DPO 연락처 등 (현재
  서버관리자_푸터관리.html이 다루는 것 전부)
- `calendar_task_completions` (user_id, task_id, completed_at) — 스마트공문달력의 173개 정적 TASKS 완료 체크

### 2.3 파일 스토리지
다음 기능들은 전부 실제 파일 업로드/다운로드가 필요하지만 현재는 파일명만 저장하거나 완전히 가짜입니다:
- 업무발송 첨부파일(다중 파일 중 1개만 저장되는 버그 있음, 3.3)
- 마이페이지 "자료 추가하기"
- 서식 자동완성 마법사 최종 산출물(HWP/HWPX/PDF)
- 공문 OCR 업로드 원본
- 스마트 계획서 변환 업로드 원본
- 프로필 아바타(현재 base64 dataURL을 localStorage에 직접 저장 — 용량 제한 있음, 실제로는 S3/Object
  Storage + CDN URL로 교체 필요)

### 2.4 AI/외부 연동
- NEIS Open API: 학사일정 조회(스마트공문달력), 학교 검색 자동완성(회원가입/프로필) — 서버 프록시로 이전.
- LLM(현재 Gemini 자리): 계획서 초안, AI 코칭, 쪽지→일정 변환(스마트공문달력), AI 상담 챗봇 응답
  (AI업무어시스턴트_챗봇.html — 현재는 100% 정적 스크립트 응답), 공문 OCR 요약, 서식 자동완성 필드 채우기.
  전부 현재 미연동이거나 연출뿐이므로 백엔드에서 사실상 처음부터 설계.
- 실시간성 요구: 업무우편함/1:1문의/멘토링/공지사항은 폴링이나 웹소켓 없이도 새로고침 기반으로 충분해
  보입니다(현재 UI에 "실시간 업데이트 중" 뱃지가 있는 화면이 일부 있으나 실제 로직은 없음 — 우선순위 낮음).

---

## 3. 페이지 밖에서 알아야 할 추가 버그 (섹션 1에 안 들어간 것들)

### 3.1 서버관리자 대시보드류는 사실상 전부 정적
서버관리자_대시보드/RAG데이터관리/리소스모니터링/보안설정, 학교관리자_대시보드/RAG데이터갱신/데이터리포트/
학교통합관리 8개 페이지의 숫자·차트·목록은 전부 하드코딩이며, 새로고침/동기화/저장 버튼 다수가 `setTimeout`
스피너 연출이거나 완전히 죽은 버튼입니다. 이 8개 페이지는 실질적으로 **디자인 목업**에 가까우므로 백엔드
설계 시 "이 화면이 원래 하려던 일"만 참고하고 현재 프론트 로직은 거의 재사용하지 않는 편이 낫습니다.
(상세는 섹션 4.6)

### 3.2 서식 자료실 마법사는 단계 간 데이터가 유실됨
서식 자동완성(서식선택 → 학교정보확인 → 생성완료)의 3단계는 각각 독립된 정적 페이지 이동(`location.href`)
일 뿐, 1단계에서 고른 템플릿이 2단계로, 2단계에서 입력한 필드가 3단계로 전달되지 않습니다. 실제로는
세션/쿼리스트링/서버 임시 저장 중 하나로 마법사 상태를 이어줘야 합니다.

### 3.3 업무발송 첨부파일 다중 업로드가 실제로는 1개만 저장됨
드롭존이 여러 파일을 선택하게 해주고 UI에는 칩으로 전부 보여주지만, 제출 시 코드는
`fileInput.files[0].name`만 읽습니다. 링크 첨부 칩도 DOM에만 추가될 뿐 제출 시 전혀 읽히지 않아 유실됩니다.
"업무 처리 시기"/"업무 발송 시기" 폼 필드도 제출 로직이 참조하지 않아 전부 무시되고 `due` 값은 항상
`'진행중'`으로 고정 저장됩니다.

### 3.4 통합대시보드류는 실제 데이터를 전혀 조회하지 않음
`인생도서관_장학사 및 업무담당자 통합 대시보드.html`은 `ef_mentoring`/`ef_inquiries` 등 실제로 쌓이고 있는
localStorage 데이터를 단 한 줄도 읽지 않고, KPI/차트/최근신청 목록이 전부 하드코딩입니다. 실제로는
`GET /api/mentoring/stats` 같은 집계 API가 필요합니다.

### 3.5 멘토링에 "담당 멘토 배정" 개념 자체가 없음
`ef_mentoring` 데이터 모델과 `인생도서관_멘토상담함.html`의 조회 로직 모두 특정 멘토에게 상담을 배정하는
개념이 없습니다 — 로그인한 멘토가 누구든 전체 상담함을 봅니다. 실서비스라면 `mentoring_requests`에
`assigned_mentor_id`(또는 카테고리 기반 자동 라우팅 규칙)를 추가하는 설계 결정이 필요합니다.

---

## 4. 섹션별 페이지 레퍼런스

각 페이지의 "목적 / 현재 데이터 소스 / 필요한 백엔드" 요약입니다. 더 상세한 라인 단위 근거가 필요하면
해당 파일을 직접 열어 이 문서의 힌트(함수명/변수명)로 검색하면 빠르게 찾을 수 있습니다.

### 4.1 업무 배송

**업무배송_스마트공문달력.html** (5,174줄, 최대 파일)
학사일정(NEIS 실시간) + 연간 업무배송 캘린더(정적 TASKS 173건) + 개인 일정 + AI 도우미 통합 달력.
`TASKS`의 마감일은 실제 날짜가 아니라 알고리즘으로 요일에 분산 배치한 값(개발자가 주석으로 "서버 구현 시
실제 배송일로 대체" 명시). Google/Outlook 캘린더 "연동"은 완전 가짜(계정명 하드코딩,
`syncNow()`는 스피너만). 필요 테이블: `personal_events`, `calendar_task_completions`,
`calendar_connections`(실제 OAuth2 구현 시). NEIS/AI는 서버 프록시로.

**업무배송_업무우편함.html**
구독 태그(30개 업무분야) 기반 수신함 + 책갈피로 개인 달력 표시. `ef_mail_data`를 업무발송.html과
공유하지만 1.1 버그로 실제 수신되는 메일은 거의 없고, 최초 12건 시드 데이터만 보임. 상세 모달 "서식
다운로드" 버튼은 완전히 죽어있음. XSS 이슈 있음(1.7).

**업무배송_업무발송.html**
업무배송 담당자 전용 발송 화면. 3.3의 다중파일/링크/시기 필드 유실 버그 있음. `dispatches` +
`dispatch_files`(+ `dispatch_categories`) 테이블 필요.

**업무배송_맞춤형업무가이드.html** *(이번 세션에 신규 구현)*
"오늘의 필수 업무" 우선순위 목록 + 이번 주 학사일정 + AI 추천 배너로 구성된 대시보드. 현재는 전부
하드코딩 예시 데이터입니다. 실제로는 로그인 사용자의 부서/역할 + 스마트공문달력의 학사일정을 기반으로
마감 임박 업무를 우선순위 정렬해 보여주는 추천 로직이 필요합니다(현재 `TASKS`/학사일정과 연결되어 있지
않음 — 신규 구현 시 함께 설계 권장).

### 4.2 인생 도서관

**인생도서관_업무체크리스트.html**
학사/회계 2개 분야 × 상반기/하반기, 대분류로 그룹핑된 자가진단 문항(품질검증 완료, 문항 데이터 자체는
개인정보 없는 공통 문항 은행). 항목별 이행(○)/미흡(△)/미이행(×) + 조치사항 텍스트를 체크할 때마다
로그인 계정과 날짜가 자동으로 배지에 기록됩니다(실명 수기 입력 없음). `ef_checklist_progress_<org>` 키
하나에 JSON 통째로 저장 — 같은 기관 소속이면 브라우저 로컬에서는 공유된 것처럼 보이지만 **실제로는
사용자 브라우저별로 독립**이라 다른 사람 화면에는 반영되지 않습니다. 실제 "같은 학교 구성원이 함께
체크"를 구현하려면 `checklist_responses(org_id, period, sector, category_id, item_no, result, action,
checked_by, checked_at)` 형태의 서버 테이블이 반드시 필요합니다. 한글(HWPX) 다운로드는 클라이언트에서
JSZip으로 base64 템플릿의 `[[문항번호]]`/`[[조치코드]]` 자리표시자를 치환하는 방식으로 완전히
동작합니다(서버 불필요, 순수 프론트 기능).

**AI업무어시스턴트_챗봇.html**
UI는 카테고리 선택 → 채팅 → AI 응답 흐름을 갖췄지만, **AI 응답은 100% 사전 작성된 고정 문자열**입니다
(`setTimeout` 후 동일한 안내 문구 출력). 실제로는 `POST /api/chat`(RAG 기반 LLM 응답) 엔드포인트가
필요합니다. 파일 첨부 UI(여러 확장자)는 이번 세션에 추가했지만 **첨부파일을 실제로 전송/분석하는 백엔드는
없음** — 현재는 파일명만 말풍선에 표시. "새 대화 시작"의 "최근 대화 내역" 목록도 정적입니다. 대화 기록을
영속화하려면 `chat_sessions`/`chat_messages` 테이블 필요.

**인생도서관_마이페이지.html**
멘토 찾기·신청 + 예약된 상담(하드코딩) + 저장한 자료(파일 바이너리 없이 파일명만 저장) + 상담 신청.
`submitMentorRequest()`가 로그인 사용자와 무관하게 이름/학교를 하드코딩해서 저장하는 버그 있음(백엔드
연동 시 세션에서 파생시키도록 수정).

**인생도서관_멘토상담함.html**
멘토 전용 상담 응답함. "월별 상담일지 출력" 모달은 인쇄 레이아웃만 있고 실제 월별 집계 로직 없음. 우측
"예약된 상담"/"최근 답변 완료" 카드는 `ef_mentoring`과 무관한 별도 하드코딩 목록.

**인생도서관_장학사 및 업무담당자 통합 대시보드.html** *(이번 세션에 접근권을 장학사 전용으로 수정)*
전체가 정적 KPI/차트(총 상담 128건, 처리완료율 92% 등)입니다. 실제 `ef_mentoring`/`ef_inquiries`
데이터를 집계해서 보여주는 통계 API가 필요합니다(3.4 참고).

### 4.3 서식 자료실

6개 페이지(통합검색 / 서식선택 / 학교정보확인 / 생성완료 / 공문OCR요약 / 스마트계획서변환) 전부
**"AI가 실시간으로 처리한다"는 문구를 쓰지만 실제로는 입력과 무관하게 동일한 결과를 보여주는 연출**입니다.
공통적으로 필요한 백엔드: 서식 템플릿 저장소(`templates` 테이블 + 실제 HWP/HWPX 바이너리 스토리지), 문서
생성 서비스(템플릿 + 사용자 입력값 병합 → 실제 파일 산출), OCR/문서이해 LLM 파이프라인, 검색 엔진(키워드 +
벡터 검색). 마법사 3단계는 상태 연결이 끊겨 있음(3.2).

### 4.4 안내센터

**안내센터_공지사항.html / 안내센터_공지사항_상세.html** *(이번 세션에 상세 페이지를 범용화)*
목록 4건(빌트인, 하드코딩) + 서버관리자가 작성한 공지(`ef_notices`, 실제 CRUD 동작). 상세 페이지는
`?id=` 쿼리로 같은 데이터를 조회하는 구조로 통일했습니다. 백엔드 연동 시 `notices` 테이블 하나로 흡수하면
됨(빌트인 4건은 시드 데이터로 이관).

**안내센터_시스템 안내.html**
매뉴얼 성격 랜딩 페이지(이번 세션에 미동작 검색창 제거). 실제 검색/문서 콘텐츠 관리가 필요하면 별도
CMS/문서 테이블 설계 필요(현재는 카드 링크 몇 개뿐).

**안내센터_1대1문의관리.html**
`ef_inquiries` 기반으로 실제 동작(등록은 이 페이지가 아니라 다른 곳에서 발생하는 것으로 추정 — 등록
폼은 이번 조사 범위에서 발견되지 않았습니다. 문의 등록 UI가 실제로 어디 있는지 확인 필요). 응답자
ID/응답시각 미기록 — `answered_by`, `answered_at` 컬럼 추가 필요.

### 4.5 공통 인증/프로필/법적고지

로그인/회원가입/프로필수정 + 4개 법적고지 페이지. 1.8, 1.9에서 다룬 인증 관련 문제 외에, 4개 법적고지
페이지(개인정보처리방침/이용약관/이메일무단수집거부/찾아오시는길)는 하단에 동일한 가짜 CMS 패턴을 공유
(`ef_footer_*`, `ef_contact_*`, `ef_dpo_*` — 서버관리자_푸터관리.html에서만 편집 가능, **브라우저별로만
저장되어 다른 사용자/기기에는 반영 안 됨**). `site_settings` 테이블로 이전 시 전 사용자에게 일관되게 반영.

### 4.6 관리자 (서버관리자 7 + 학교관리자 4)

**실제 데이터 연동이 있는 것**: 서버관리자_푸터관리.html(완전 동작), 서버관리자_역할관리.html(동작하나
1.2/1.3의 스키마 정리 필요), 서버관리자_회원관리.html(1.1/1.4의 버그로 사실상 미동작).

**나머지 8개는 전부 정적 목업**: 서버관리자_대시보드(인프라 현황), RAG데이터관리(문서 업로드는 진행률바만
가짜로 재생), 리소스 모니터링(CPU/메모리 등 `Math.random()`으로 3초마다 흔들리는 가짜 실시간),
보안설정(IP allow/deny list, MFA/비밀번호 정책 토글 — 전부 `console.log`만 함), 학교관리자_대시보드,
RAG데이터갱신(업로드 UI조차 없는 완전 빈 페이지), 데이터 리포트(Excel/PDF 버튼 무반응), 학교통합관리
(인력배분 AI 추천 "적용하기" 버튼 무반응).

이 8개는 백엔드 설계 시 "화면이 원래 보여주려던 지표가 무엇인가"만 참고하고, 실제 쿼리/집계 로직은
처음부터 설계하는 것을 권장합니다. 특히 보안설정.html은 실제로 구현되면 서버 보안 정책(IP allowlist 등)에
직결되므로 우선순위 높게 검토 필요.

### 4.7 홈 (index.html) *(이번 세션에 위젯 시스템 신규 구현)*

히어로 배너 + 사용자 맞춤 위젯 그리드. 위젯 6종(업무배송 캘린더/인생도서관/한글파일뚝딱/서식자료실/
업무체크리스트 진행률/최근 공지사항) 중 업무체크리스트 진행률 위젯만 실제 `ef_checklist_progress_<org>`를
읽어 표시하고, 나머지는 정적 예시 콘텐츠입니다. 배치(선택/순서/크기)는 `ef_home_widgets_<email>`에
저장됩니다. 백엔드 연동 시 `home_widget_layouts` 테이블로 이전하면서, 위젯 콘텐츠 자체도 각 위젯이 대표하는
실제 데이터(예: 캘린더 위젯은 스마트공문달력의 이번 주 일정, 서식자료실 위젯은 최근 등록 서식 API)로
교체하는 2단계 작업이 필요합니다.

---

## 5. 알려진 미구현/장식 기능 총정리

빠르게 스캔할 수 있도록 "버튼은 있는데 아무 일도 안 일어나는 것" 위주로 모았습니다(위 섹션에서 이미 다룬
것 포함, 중복 감수하고 한곳에 모음).

- 로그인: "비밀번호 찾기", "교육기관 인증서로 로그인" — 죽은 링크/버튼
- 회원가입: 이메일 인증 PIN이 6자리 숫자면 무엇이든 통과(실제 대조 없음)
- 프로필수정.html: 전체가 도달 불가능(사실상 profile-modal.js가 대체) + "변경사항 저장" 버튼 무반응
- 업무우편함: 상세 모달 "서식 다운로드" 무반응
- 업무발송: 다중파일 2번째부터 유실, 링크첨부 유실, 발송시기/처리시기 선택 무시됨
- 스마트공문달력: Google/Outlook 연동 100% 가짜, CSV 내보내기 코드 있으나 주석 처리되어 비활성
- 서식 통합검색: 검색/필터/정렬/페이지네이션 전부 무반응
- 서식선택(STEP1): 카테고리 필터/탭/미리보기/업로드 전부 무반응, 카드 6개가 전부 같은 곳으로 이동
- 학교정보확인(STEP2): "초기화"/확대축소 무반응, 마법사 단계 간 입력값 유실
- 생성완료(STEP3): 다운로드가 스피너 연출뿐, 실제 파일 없음
- 공문OCR요약: 업로드 내용과 무관하게 고정 요약 출력, 필터/재시도/상세보기/FAB 무반응
- 스마트계획서변환: 업로드 파일을 읽지 않고 고정 변환 결과 출력, 다운로드는 알림만
- 마이페이지: "다운로드"/"회의실 입장하기" 토스트만 뜸
- 멘토상담함: "상담 가능 시간 설정" 완전 죽은 버튼
- 통합대시보드: "편람 업데이트 바로가기"/"전체보기"/"신청 목록 전체 보기" 죽은 링크
- 안내센터_시스템 안내: (이번 세션에 미동작 검색창 제거 완료)
- 서버관리자 8개 + 학교관리자 4개 대시보드류: 섹션 4.6 참고, 대부분의 버튼/토글이 `console.log` 또는 무반응
- `main.html`, `main2.html`: 어디서도 링크되지 않는 **고아 파일**(초기 디자인 목업 잔재로 보임). 삭제
  여부 검토 권장. `school-self-check-temp/`, `find_block.py`, `fix_sync.py`, `port_checklist.py`도 이전
  세션의 이식 작업용 스크래치 파일로, git에 커밋되지 않은 미추적 상태 — 정리 대상인지 확인 필요.

---

## 6. localStorage 키 전체 인벤토리 (부록)

| 키 | 모양 | 사용처 |
|---|---|---|
| `currentUser` | 문자열(이메일) | 전역(auth.js) |
| `currentRole` | 문자열(한글 역할명) | 전역(auth.js) |
| `grantedRoles_<email>` | 배열 | auth.js, profile-modal.js, 서버관리자_역할관리.html |
| `grantedRoles` (접미사 없음) | 객체 | **member-management.js만 사용 — 1.3 참고, 다른 곳과 불일치** |
| `roleRequests` | 배열 `[{email, requestedRole}]` | 회원가입, profile-modal.js, 역할관리.html |
| `roleRequests` (같은 키, 다른 모양) | 객체 `{email: [role]}` | **member-management.js만 이렇게 읽음 — 1.2 참고** |
| `org_<email>`, `rank_<email>` | 문자열 | profile-modal.js(단, 저장 로직 누락 — 1.9) |
| `avatar_<email>` | base64 dataURL | profile-modal.js |
| `ef_inquiries` | 배열 | assets/js/inquiries.js |
| `ef_mentoring` | 배열 | assets/js/mentoring.js |
| `ef_checklist_progress_<org>` | 중첩 객체(period→sector→category→item) | 인생도서관_업무체크리스트.html |
| `ef_notices` | 배열 | 안내센터_공지사항.html, _상세.html |
| `ef_home_widgets_<email>` | 배열 `[{id, size}]` | index.html |
| `ef_mail_data` | 배열 | 업무배송_업무발송.html, _업무우편함.html |
| `ef_mailSubscriptions_<user>` | 배열 | 업무배송_업무우편함.html |
| `ef_mailCalendarBookmarks_<user>` | 배열 | 업무배송_업무우편함.html |
| `ef_events`, `ef_done`, `ef_settings`, `ef_school`, `ef_depts`, `ef_ai_cache`, `ef_sideopen` | 각각 다름 | 업무배송_스마트공문달력.html |
| `ef_saved_resources` | 배열 | 인생도서관_마이페이지.html |
| `ef_upcoming_appointments` | 배열 | 인생도서관_마이페이지.html |
| `ef_footer_org`, `ef_footer_address`, `ef_footer_copyright` | 문자열 | 전 페이지 푸터, 서버관리자_푸터관리.html에서 편집 |
| `ef_contact_dept`, `ef_contact_phone`, `ef_contact_email` | 문자열 | 개인정보처리방침 등, 서버관리자_푸터관리.html에서 편집 |
| `ef_dpo_name`, `ef_dpo_title`, `ef_dpo_phone` | 문자열 | 개인정보처리방침, 서버관리자_푸터관리.html에서 편집 |
| `mockUsers` | 배열(시드 5명) | member-management.js 전용, 실데이터 아님 |

---

## 7. 이 문서의 한계

- 이번 조사는 3개 병렬 에이전트 + 직접 작업한 세션 지식을 종합한 것으로, 매우 구체적인 라인 번호까지
  전부 재검증하지는 않았습니다(특히 섹션 4의 서식자료실/관리자 부분은 원 조사 결과를 요약한 것이므로,
  실제 구현 착수 전 해당 파일을 직접 열어 함수명 기준으로 재확인 권장).
- 콘텐츠 자체의 정책적 정확성(예: 개인정보처리방침의 "고려대학교 공공정책연구소 위탁" 문구 등 실제
  법무 검토가 필요한 텍스트)은 이 문서의 범위 밖입니다.
- AI(챗봇, OCR, 서식자동완성, 계획서변환)의 실제 모델/프롬프트 설계는 다루지 않았습니다 — 이 문서는
  "현재 프론트가 이 기능을 어떤 입출력 형태로 기대하는가"까지만 정리했습니다.
