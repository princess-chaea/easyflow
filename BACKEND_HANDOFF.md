# 이지플로우(EasyFlow) 백엔드 연동 가이드

이 문서는 현재 정적 HTML/Tailwind 프로토타입(빌드 시스템 없음, 백엔드 없음, localStorage·명시적 데모 데이터 중심이며
일부 기능은 동일 출처 API 프록시 계약을 선반영)으로 만들어진 프론트엔드를 실제 백엔드에 연결하기 위한 참고 자료입니다. 백엔드 담당자가 이 문서만
보고도 "지금 무엇이 진짜 동작하고 무엇이 가짜인지", "어떤 데이터를 어떤 모양으로 저장해야 하는지"를 파악할 수
있도록 정리했습니다.

> **2026-08-15 인계 기준**: 현재 상태와 구현 계약은 이 문서의 최신 상태 표, `BACKEND_API_CONTRACT.md`,
> `DOCUMENT_AGENT_SPEC.md`를 우선한다. 과거 오류 분석은 회귀 방지 기록이며 현재 동작으로 해석하지 않는다.

- API 요청·응답·오류·파일 정책: [`BACKEND_API_CONTRACT.md`](BACKEND_API_CONTRACT.md)
- 열려 있는 HWP/HWPX/ODT 계획·수정 모드: [`DOCUMENT_AGENT_SPEC.md`](DOCUMENT_AGENT_SPEC.md)
- 프론트 협의용 데모 흐름과 운영 전환: [`FRONTEND_REVIEW_GUIDE.md`](FRONTEND_REVIEW_GUIDE.md)

최초 조사: 2026-08-05 / 최신 검증: 2026-08-15. 프론트엔드 전체 39개 페이지(관리자 11 + 업무배송 4 + 서식자료실 6 + 인생도서관 5 +
안내센터 3 + 공통 인증/법적고지 8 + 홈 1 + 챗봇 1) 및 `assets/js/` 공유 모듈 6개를 전수 조사했습니다.

**2026-08-06 업데이트**: 아래 항목 중 "백엔드 연결과 무관한 프론트엔드 자체 버그"였던 6건(1.1, 1.2, 1.3,
1.7, 1.9, 3.2, 3.3 — 정확히는 1.1/1.7/1.9/3.3이 한 그룹, 1.2·1.3이 한 그룹, 3.2가 한 그룹)은 **모두 수정
완료**되었습니다. 각 항목 안에 ✅ 표시와 커밋 설명을 남겨두었으니, 아직 남아 있는 "백엔드가 있어야 해결되는"
항목(1.5, 1.6 등)과 구분해서 보시면 됩니다.

**2026-08-07 업데이트 (전수 UI/UX 점검)**: 그동안 상대적으로 덜 손봤던 화면들 — 서버관리자 대시보드류 4개,
학교관리자 대시보드류 4개, 서식 자료실 6개, 인생도서관 통합대시보드 — 을 병렬로 전수 점검하여 "버튼은 있는데
아무 일도 안 일어나는" 죽은 UI를 최대한 실제 동작(또는 최소한 정직한 피드백)으로 바꿨습니다. 핵심 원칙은
①이미 쌓이고 있는 실제 localStorage 데이터(roleRequests/ef_inquiries/ef_mentoring/ef_notices/
ef_mentor_roster/ef_checklist_progress_&lt;org&gt;/ef_consult_logs)로 대체 가능한 하드코딩 수치·목록은
실데이터로 교체, ②CPU/메모리 등 진짜 백엔드 없이는 불가능한 값은 그대로 두되 클릭 시 `EF_MODAL`로 정직한
피드백을 주도록, ③클라이언트에서 진짜로 만들 수 있는 산출물(CSV 내보내기, 검색/필터/정렬, 폼 데이터를 채운
HTML 문서 다운로드)은 실제로 동작하게 만드는 것이었습니다. 상세 내용은 섹션 3.1/4.2/4.3/4.6/5/6에 반영.
이번 점검에서 로그인 화면의 죽은 링크(비밀번호 찾기/교육기관 인증서 로그인), 마이페이지 "저장한 자료"
목록의 저장형 XSS(파일명이 이스케이프 없이 innerHTML에 삽입), 스마트공문달력의 CSV 내보내기(코드는
완성돼 있었으나 통째로 주석 처리되어 비활성 상태였음), 1:1 문의 답변자·답변일 미기록도 함께 고쳤습니다.
또한 어디서도 링크되지 않던 고아 파일(`main.html`, `main2.html`, 배치 수정용 스크래치 스크립트 20여 개,
`school-self-check-temp/`)을 저장소에서 정리했습니다.

**2026-08-08 업데이트 (마무리 전수 점검)**: 서식 자동완성 STEP1(카드 클릭이 비교 없이 바로 다음 단계로
넘어가던 것을 선택→확인 절차로 변경)·STEP2(계정 소속 정보로 학교명 자동 입력, 4.3.1 참고)를 고친 뒤,
그동안 상대적으로 덜 점검했던 나머지 화면 전체(공통 인증/법적고지 8개, 홈, 업무배송 3개, 인생도서관 2개,
AI챗봇, 안내센터 2개, 서버관리자 3개)를 병렬로 마지막 점검했습니다. 이번 라운드는 새 기능 추가보다
**"이미 완성됐어야 하는데 사소한 이유로 죽어있던" 버그**를 주로 찾았습니다 — 특히 같은 패턴이 여러 파일에서
반복 발견됐습니다: **①복붙 잔재로 남은, 아무도 열어주지 않는 고아 모달/스크립트**(AI챗봇의 "월별 상담일지
출력" 모달 — 멘토상담함.html에서 그대로 복사된 무관한 하드코딩 데이터 포함, 스마트공문달력의 "초기화 확인"
모달)와 **②id 중복**(스마트공문달력의 Outlook 인증만료 섹션이 구글 쪽과 똑같은 `id="stateExpired"`를 써서
`getElementById`가 항상 구글 쪽만 찾던 잠재 버그)이 이번에도 반복 확인됨. 그 외 회원가입 페이지의
`<head>`/`<body>` 통째 중복 렌더링 버그, 역할관리의 XSS, 회원관리의 검색/필터 완전 미연동, 공지사항 삭제
기능 부재 등도 발견해 수정. 상세는 각 섹션에 인라인으로 반영하지 않고 커밋 로그(`git log`)를 참고하시면
됩니다 — 이 라운드는 새로운 백엔드 요구사항을 추가하지 않았고(전부 프론트 자체 버그), 섹션 5의 "알려진
미구현/장식 기능" 목록에도 변동이 없습니다(이번에 고친 건 전부 "원래 완성됐어야 했는데 죽어있던" 것들이라
장식 기능 목록과는 다른 종류의 문제였습니다).

---

**2026-08-08 인계 정합성 최신화**: 1.1~1.3, 1.7, 1.9, 3.2~3.4, 4.1~4.6의 일부 본문은
수정 전 원인 기록을 보존한다. 제목 또는 인라인의 ✅가 붙은 항목은 현재 프론트에서 수정 완료된 이력이며,
백엔드는 그 과거 버그를 재현·호환할 필요가 없다. 현재 미구현은 실제 인증·공유 DB·파일 스토리지·AI/RAG·실측
인프라 지표이며, 아래 최신 상태 요약과 섹션 2의 정규화 모델을 우선 기준으로 삼는다.

## 0-A. 2026-08-16 최종 점검에서 반영한 변경

- 회계 체크리스트에서 빠진 `4-1_1` 항목을 복원했다.
- HWPX 공식 양식에서 여러 하위 점검이 한 결과 칸을 공유하는 경우 `X > △ > 미입력 > O` 순으로 집계하고, 조치 내용은 중복 제거 후 합쳐서 내보낸다.
- 회계 HWPX의 서로 다른 두 행이 같은 `7-4_21` 자리표시자를 쓰는 문제를 내보내기 직전에 독립 키로 정규화한다.
- HWP/HWPX처럼 표시하면서 실제 HTML을 내려주던 두 화면은 `HTML 미리보기`로 명확히 표시하고 HWPX 버튼을 백엔드 연동 전까지 비활성화했다.
- 스마트 계획서의 끊긴 `인생도서관_업무가이드.html` 링크를 실제 체크리스트 페이지로 수정했다.
- 챗봇 멘토 신청이 성공 문구만 표시하던 흐름을 `EF_MENTORING.add()`에 연결했다.
- 저장된 챗봇 HTML과 추후 AI HTML 응답에 허용 목록 정화를 추가했다.
- 브라우저에 있던 NEIS 키와 Gemini 직접 호출을 제거하고 `/api/neis/*`, `/api/ai/generate` 프록시 계약으로 바꿨다.
- 프론트 협의회에서 흐름이 끊기지 않도록 학교 검색·학사일정·달력 AI에 `프록시 우선 → 실패 시 명시적 데모 폴백`을 추가했다. 운영에서는 `window.EF_CONFIG.demoFallback = false`로 끈다.
- 스마트 계획서의 교사 화면을 `예시 열기 → 바꿀 내용 적기 → 수정할 곳 고르기 → 문장 직접 수정 →
  결과 미리보기`로 확정했다. JSON·패치·리비전·영수증·I/O 같은 개발 용어와 내부 다운로드 버튼은
  사용자에게 숨겼다. 내부 `DocumentPatch`·`DocumentApplyReceipt` 계약은 백엔드 연결을 위해 유지한다.
- `rhwp` 정적 배포본과 `@rhwp/editor 0.8.2`를 웹앱에 자체 호스팅해 실제 HWP/HWPX 파일 열기·편집,
  HWP/HWPX 수정본 저장을 구현했다. 문서 바이트는 브라우저 안에서 동일 출처 iframe으로 전달되고 제3자
  편집 서버나 EasyFlow API로 업로드되지 않는다. 반입 파일·해시·MIT/폰트 고지는
  `THIRD_PARTY_NOTICES.md`에 기록했다.
- 문서 처리 GitHub 후보의 기술 역할과 라이선스 도입 금지선을 `DOCUMENT_AGENT_SPEC.md` 14~15절에 정리했다.
  BSL은 비프로덕션 평가로 제한하고, 라이선스 미확인 저장소는 코드가 아니라 개념만 독립 구현한다.
- 회원가입에서 학교 검색 스크립트가 중복 로드되어 `EF_SCHOOL`을 재선언하던 오류를 공통 로더에서 차단했다.
- 검색 UI가 제거된 시스템 안내 페이지에서 존재하지 않는 입력 요소에 이벤트를 연결하던 런타임 오류를 제거했다.
- 업무 체크리스트 HWPX 내보내기의 깨진 결과 기호 문자열을 `×`·`△`·`○`으로 복원했다.
- 로그인 폼으로 세션을 만든 뒤 전체 HTML 39개를 Edge에서 다시 열어 확인했다. 로그인 이탈·빈 화면·탐색 오류·
  JavaScript 런타임 예외는 각각 0건이었다. 중복 DOM `id`와 실제 깨진 상대경로도 각각 0건이다.

---
## 0. 가장 먼저 읽을 것 — 실제 동작과 백엔드 필요 기능의 경계

프로토타입에는 브라우저만으로 끝까지 동작하는 흐름과 실제 인증·공유 DB·문서 엔진이 있어야 완성되는 흐름이
함께 있다. 아래 표는 **현재 상태가 실제 저장인지, 프록시 우선 데모인지, 계약 검토용 UI인지**를 구분하기 위한
기준이다. 표에 없는 버튼·업로드·차트·AI 응답은 페이지별 섹션에서 별도 확인해야 하며 실제 서버 연동으로
추정해서는 안 된다.

| 기능 | 관련 파일 | 저장 키 |
|---|---|---|
| 로그인/로그아웃/역할 표시 | 공통_로그인.html, assets/js/auth.js | `currentUser`(문자열), `currentRole` |
| 역할 추가 신청 → 승인 | 공통_회원가입.html, profile-modal.js, 서버관리자_역할관리.html | `roleRequests`(배열), `grantedRoles_<email>` |
| 1:1 문의 등록/답변 | 안내센터_1대1문의관리.html, assets/js/inquiries.js | `ef_inquiries` |
| 멘토링 신청/답변 | 인생도서관_마이페이지.html, 인생도서관_멘토상담함.html, assets/js/mentoring.js | `ef_mentoring` |
| 화상 상담 예약(슬롯 설정/예약/취소, 요일 반복) | 인생도서관_마이페이지.html, 인생도서관_멘토상담함.html, assets/js/mentoring.js | `ef_mentor_slots`, `ef_appointments` |
| 멘토-업무분야 배정(다중 멘토 라우팅) | 서버관리자_멘토배정관리.html, assets/js/mentoring.js | `ef_mentor_roster` |
| 상담 일지 작성 + 월별 출력 | 인생도서관_멘토상담함.html, assets/js/mentoring.js | `ef_consult_logs` |
| 업무 체크리스트 점검 + 내장 양식 HWPX 생성 | 인생도서관_업무체크리스트.html | `ef_checklist_progress_<org>` |
| AI 챗봇 최근 대화(프론트 임시) | AI업무어시스턴트_챗봇.html | `ef_chat_history_<email>` |
| 공지사항 작성(서버관리자) | 안내센터_공지사항.html | `ef_notices` |
| 홈 위젯 배치 | index.html | `ef_home_widgets_<email>` |
| 프로필 아바타 업로드 | profile-modal.js | `avatar_<email>` |
| 푸터/연락처/DPO 정보 편집 | 서버관리자_푸터관리.html | `ef_footer_*`, `ef_contact_*`, `ef_dpo_*` |
| 업무발송 → 업무우편함 | 업무배송_업무발송.html, 업무배송_업무우편함.html | `ef_mail_data` (1.1의 크래시 버그 2026-08-06 수정 완료 - 이제 정상 동작) |
| 업무우편함 구독/책갈피 | 업무배송_업무우편함.html | `ef_mailSubscriptions_<user>`, `ef_mailCalendarBookmarks_<user>` |
| 학사캘린더 개인 일정/완료 체크 | 업무배송_스마트공문달력.html | `ef_events`, `ef_done`, `ef_settings` 등 |
| NEIS 학교·학사일정 조회 | 업무배송_스마트공문달력.html, school-search.js | 동일 출처 프록시 우선, 실패 시 `데모` 표시가 붙은 협의용 데이터 |
| 달력 AI 기안·코칭·브리핑·쪽지 일정 추출 | 업무배송_스마트공문달력.html | `/api/ai/generate` 우선, 로컬 파일/명시적 개발 설정에서만 협의용 데모, 텍스트 결과 캐시는 `ef_ai_cache` |
| 계획서 예시 다듬기 UI | 서식자료실_스마트 계획서 변환.html | 요청·수정 항목 선택·제안 문장 직접 수정·반영/원래대로·결과 미리보기 실동작. 내부 계약 JSON은 사용자에게 숨김 |
| HWP/HWPX 웹 편집 | 서식자료실_스마트 계획서 변환.html, assets/js/web-hwp-editor.js, rhwp/, assets/vendor/rhwp-editor/ | 자체 호스팅 브라우저 편집·HWP/HWPX 수정본 저장 실동작. 서버 업로드 없음 |

나머지 화면에도 검색·필터·CSV·localStorage 기반 상호작용은 일부 있지만, 서버 인증·공유 DB·파일 바이너리 저장·
실제 OCR/LLM/RAG·실측 인프라 지표는 없다. 서식자료실 스마트 계획서의 자체 호스팅 웹 편집기와 체크리스트
HWPX 내보내기는 예외적으로 브라우저에서 실제 문서 바이트를 처리한다. 그 밖의 HTML 미리보기는 실제 HWP/HWPX
생성으로 간주하면 안 된다. 섹션 4의 페이지별 상태와 0-A의 최신 변경을 함께 확인한다.

---

## 1. 백엔드 설계 전에 반드시 알아야 할 크로스커팅 이슈

아래 항목들은 특정 페이지 하나의 문제가 아니라 **여러 페이지가 공유하는 데이터 모델/컨벤션이 서로 어긋나 있는
문제**입니다. 백엔드 스키마를 페이지별로 그대로 베끼면 이 불일치까지 같이 이식되므로, 먼저 어느 쪽을
"정답"으로 통일할지 결정하고 시작하는 것을 권장합니다.

### 1.1 `currentUser` 값 처리 불일치 — ✅ 2026-08-06 프론트 수정 완료, 인증 전환 필요

> **현재 상태 (2026-08-08)**: 아래 내용은 수정 전의 원인 기록이다. 현재 업무발송은 이메일을 JSON으로
> 파싱하지 않고, `org_<email>`에서 발신 소속을 읽어 `ef_mail_data`에 정상 저장한다. 백엔드는 이 과거
> 데이터 형태를 호환할 필요가 없으며, 세션/토큰의 `/users/me`로 사용자·소속·부서·직위를 제공하면 된다.

- `공통_로그인.html`과 `assets/js/auth.js`는 `currentUser`를 **순수 문자열(이메일)**로 저장/사용합니다.
  (`localStorage.setItem('currentUser', email)`)
- 그런데 `업무배송_업무발송.html`(제출 로직)과 `assets/js/member-management.js`(서버관리자_회원관리.html)는
  `JSON.parse(localStorage.getItem('currentUser'))`로 **JSON으로 파싱을 시도**합니다.
- 이메일 문자열은 유효한 JSON이 아니므로 `JSON.parse("hong@gbe.kr")`는 예외를 던집니다. try/catch가 없어서
  **함수 전체가 조용히 중단**됩니다 — 사용자에게는 에러도 성공 메시지도 뜨지 않고, 버튼이 반응 없는 것처럼
  보입니다.
- 실질적 영향: **업무배송 담당자가 업무발송을 제출해도 `ef_mail_data`에 아무것도 추가되지 않습니다.**
  이것이 이전부터 알려진 "업무발송 → 업무우편함이 연결 안 됨" 문제의 정확한 원인입니다(설계는 같은 키를
  공유하도록 되어 있었으나 이 버그로 실행 전에 죽습니다).
- 추가로 `업무배송_업무발송.html`은 `currentUser.dept`(부서) 필드도 참조하는데, 이 필드는 회원가입 폼에
  애초에 존재하지 않습니다 — 버그를 고쳐도 발신 부서는 항상 `'해당부서'`로 고정 표시됩니다.
- **백엔드 조치**: 실제 인증을 세션/JWT로 교체하면서 "현재 로그인 사용자" 정보를 서버가 내려주는 구조로
  바뀌면 이 문제는 자연히 해소됩니다. 다만 프론트가 이 값을 참조하는 모든 지점(위 두 파일 포함)을 새 인증
  방식에 맞게 같이 고쳐야 합니다.

### 1.2 `roleRequests` 데이터 형태 불일치 — ✅ 2026-08-06 프론트 수정 완료

> **현재 상태 (2026-08-08)**: 역할 요청은 `[{email, requestedRole}]` 배열로 통일됐다. 회원가입·프로필·역할관리·회원관리가
> 같은 데이터를 읽고, 회원관리 승인도 요청 제거와 `grantedRoles_<email>` 반영을 함께 수행한다. 아래는 수정 전 불일치 기록이다.

- `공통_회원가입.html`, `profile-modal.js`, `서버관리자_역할관리.html` — **배열**로 사용:
  `[{email, requestedRole}, ...]`
- `assets/js/member-management.js`(서버관리자_회원관리.html) — **객체**로 사용:
  `{ "email@x.com": ["role1", "role2"], ... }`
- 결과: 회원가입에서 실제로 신청이 들어와도, 회원관리 화면의 승인 UI는 이 데이터를 절대 읽지 못합니다
  (`roleRequests[user.email]`가 항상 `undefined`).
- **백엔드 조치**: `role_requests` 테이블(id, user_id, requested_role, status, created_at)로 정규화하고,
  두 화면 모두 이 테이블 하나만 보도록 통일. 프론트 두 곳의 로컬 로직은 모두 폐기.

### 1.3 `grantedRoles` 키 규칙 불일치 — ✅ 2026-08-06 수정 완료 (`grantedRoles_<email>`로 통일)

> **현재 상태 (2026-08-08)**: 승인 역할은 사용자별 `grantedRoles_<email>` 배열로 통일됐다. 회원관리에서 승인한
> 역할도 `EF_ROLE.effective()`의 실제 권한 판정에 반영된다. 아래는 수정 전 전역 키 사용 문제의 기록이다.

- `auth.js`(실제 권한 판정에 사용), `profile-modal.js`, `서버관리자_역할관리.html` — 사용자별 키:
  `grantedRoles_<email>` (JSON 배열)
- `assets/js/member-management.js` — **전역** 키 `grantedRoles` (이메일 구분 없음)
- 결과: `서버관리자_회원관리.html`에서 "승인" 버튼을 눌러도 실제 권한 판정 로직(`EF_ROLE.effective()`)이
  보는 저장소에는 반영되지 않습니다. **역할관리.html이 실제로 동작하는 승인 화면이고, 회원관리.html의 승인
  UI는 눈속임에 가깝습니다.**
- **백엔드 조치**: 1.2와 함께 `role_requests`/`user_roles` 테이블로 통합. 서버관리자_회원관리.html과
  역할관리.html이 같은 데이터를 다른 뷰로 보여주는 형태로 재설계 필요.

### 1.4 서버관리자_회원관리.html은 회원관리.html·역할관리.html 둘 다와 사실상 무관한 별도 목업 (부분 수정)

**2026-08-06 업데이트**: 1.2/1.3의 스키마 불일치는 고쳐서, 이제 회원관리.html에서 승인하면 실제로
`grantedRoles_<email>`에 반영되고(=auth.js가 진짜로 읽는 값) 역할관리.html의 대기 목록과도 같은 데이터를
봅니다. **다만 `mockUsers`(회원 목록 자체)는 여전히 가짜 5명 + 현재 로그인 계정만 들어있는 하드코딩
데이터**입니다 — 이 부분은 실제 `users` 테이블이 있어야 해결되는 문제라 아래 원래 설명대로 남겨둡니다.

- 회원관리.html이 표시하는 회원 목록(`mockUsers`, 5명, `@test.com` 가짜 계정)은 실제 가입자 데이터와
  전혀 연결되어 있지 않습니다. 헤더의 "전체 회원 1,248명" 같은 통계도 고정 텍스트입니다.
- **백엔드 조치**: `users` 테이블이 생기면 이 페이지는 그 테이블을 페이지네이션 조회하는 화면으로
  완전히 새로 구현해야 합니다. 현재 프론트 코드는 참고용으로도 거의 재사용 가치가 없습니다(스키마가 실제
  요구사항과 다름).

### 1.5 권한 검사가 전적으로 클라이언트 사이드

- `assets/js/auth.js`의 `PAGE_ROLE_REQUIREMENTS`/`ROLE_GATED_LINKS`가 페이지 접근·메뉴 노출을 제어하지만,
  이건 브라우저 devtools에서 `localStorage.setItem('currentRole', '서버관리자')` 한 줄이면 누구나 우회
  가능합니다.
- `assets/js/inquiries.js`, `assets/js/mentoring.js`의 `add()`/`answer()`/`respond()` 같은 데이터 함수
  자체에도 호출자 권한 검사가 전혀 없습니다 — 콘솔에서 아무나 `EF_INQUIRY.answer(1, '아무거나')` 호출 가능.
- 정적 파일 서빙 특성상 **서버가 렌더링에 관여하지 않으므로, 관리자 페이지의 HTML/JS 자체는 이미 누구에게나
  전달됩니다.** 데이터가 실제 API로 옮겨가면 이 문제는 훨씬 심각해집니다(단순 UI 숨김이 아니라 진짜 데이터
  유출/변조 가능).
- **백엔드 조치**: 모든 API 엔드포인트는 프론트의 역할 문자열을 신뢰하지 말고, 서버 세션에 저장된 역할로
  자체 인가(authorization)를 수행해야 합니다. `PAGE_ROLE_REQUIREMENTS`의 매핑표는 "어떤 페이지에 어떤
  역할이 필요한가"에 대한 **요구사항 명세**로만 재사용하고, 실제 검사는 서버에서 새로 구현.

### 1.6 외부 API 키 프론트 노출 — ✅ 2026-08-15 프론트 제거 완료, 기존 키 폐기 필요

- `업무배송_스마트공문달력.html`과 `assets/js/school-search.js`에 있던 NEIS 키는 소스에서 제거했다.
- 달력의 Gemini 공급자 직접 호출과 키 플레이스홀더도 제거했다.
- 현재 프론트 계약은 `GET /api/neis/schools`, `GET /api/neis/schedules`, `POST /api/ai/generate`이며 세부 응답은 `BACKEND_API_CONTRACT.md`를 따른다.
- **운영 조치**: 저장소 이력이나 배포본에 노출됐던 기존 NEIS 키는 폐기·재발급한다. 서버는 비밀 저장소, 호출 제한, 타임아웃, 캐시, 감사 로그를 적용한다.

### 1.7 XSS: 업무우편함이 이스케이프 없이 innerHTML 사용 ✅ 2026-08-06 수정 완료

> **현재 상태 (2026-08-08)**: 업무우편함과 AI 챗봇의 사용자 입력은 텍스트·속성값 이스케이프가 적용됐고,
> 업무 링크는 http/https만 허용한다. 아래는 수정 전 취약점과 재발 방지를 위한 기록이다. API 응답도 같은
> 렌더링 규칙을 지키고, 서버는 입력 길이·형식·URL·파일명을 검증해야 한다.

- `업무배송_업무우편함.html`의 `renderMailList()`는 메일의 `title/desc/file/dept/category` 값을 이스케이프
  없이 문자열 결합으로 `innerHTML`에 삽입합니다(속성값에도 따옴표 이스케이프 없음).
- 이 값들은 `업무배송_업무발송.html`에서 **업무배송 담당자가 직접 입력하는 제목/설명/업로드 파일명**이므로,
  악의적인(혹은 실수로 HTML 특수문자가 들어간) 제목이 저장형 XSS로 이어질 수 있습니다.
- 같은 세션에서 이미 고친 사례가 있었습니다: `AI업무어시스턴트_챗봇.html`은 원래 사용자가 입력한
  채팅 텍스트를 그대로 `innerHTML`에 꽂고 있었는데, `escapeHtml()` 헬퍼를 추가해 수정했습니다.
  **2026-08-06에 같은 패턴(속성값까지 안전한 `&<>"'` 전체 이스케이프 버전)을 업무우편함.html에도
  적용**했고, 링크는 http/https 프로토콜만 렌더링하도록 발신·수신 양쪽에서 검증을 추가했습니다.
- **백엔드 조치**: 프론트 쪽 렌더링 이스케이프는 고쳤지만, API가 반환하는 텍스트 필드를 프론트가 항상
  이스케이프 후 렌더링하는 규칙은 앞으로 새 화면을 만들 때도 계속 지켜야 합니다. 서버 쪽에서도 저장 시
  악성 스크립트 패턴을 막는 입력 검증은 방어선으로 추가하는 게 안전합니다.

### 1.8 신규 가입자는 영구히 로그인할 수 없음

- `공통_회원가입.html`은 제출 시 `roleRequests`에만 기록하고, **계정 자체(이메일/비밀번호)는 어디에도
  저장하지 않습니다.**
- `공통_로그인.html`은 하드코딩된 8개 테스트 계정(`test@gbe.kr` ~ `test7@gbe.kr`, 공용 비밀번호
  `test1234!`)만 인식합니다.
- 이메일 인증 PIN도 가짜입니다 — `공통_회원가입.html`의 PIN 검증은 **6자리 숫자이기만 하면 무엇이든 통과**
  시킵니다(실제 발송된 코드와 대조하지 않음).
- **백엔드 조치**: `POST /auth/signup`(계정 생성 + 비밀번호 해싱), `POST /auth/send-verification` +
  `POST /auth/verify-pin`(실제 발송/대조), `POST /auth/login`(세션/JWT 발급)이 모두 새로 필요합니다.
  로그인 페이지의 하드코딩 8계정과 "[개발용] 빠른 역할 선택" 드롭다운(코드 주석에 이미 "추후 제거" 명시)은
  실배포 전 반드시 제거.

### 1.9 프로필 수정(소속·직위)이 저장되지 않는 버그 ✅ 2026-08-06 수정 완료
> **2026-08-08**: 실제 모달은 소속·직위를 저장하며, 정적 프로필 URL도 `index.html?openProfile=1`로 연결했다.

- `profile-modal.js`가 주입하는 프로필 모달에서 소속/직위 입력란은 있지만, "확인 및 닫기" 버튼
  핸들러가 모달을 닫기만 할 뿐 `org_<email>`/`rank_<email>`에 값을 저장하는 로직이 없습니다. 아바타
  업로드만 즉시 저장됩니다.
- 참고로 `공통_프로필수정.html` 파일 자체는 **도달 불가능한 죽은 페이지**입니다 — `profile-modal.js`가
  페이지 로드 시 이 링크와 설정(톱니바퀴) 아이콘을 DOM에서 제거하고, "프로필 설정" 클릭을 가로채 자체
  모달을 띄웁니다. 실제 프로필 편집 화면의 스펙은 이 HTML 파일이 아니라 `profile-modal.js`가 주입하는
  모달 쪽을 기준으로 삼아야 합니다.
- **백엔드 조치**: `PATCH /users/me {org, rank}` 필요. 프론트 저장 누락 버그는 백엔드 연동 시 같이 수정.

---

## 2. 제안하는 백엔드 아키텍처 개요

### 2.1 인증
- 세션 쿠키 또는 JWT 중 택1. 현재 `currentUser`/`currentRole`을 그대로 대체.
- 역할(교직원/장학사/업무배송 담당자/멘토/학교관리자/서버관리자) — 정확히는 base role(가입 시 자동
  "교직원") + 승인받은 추가 역할(복수 가능, `grantedRoles`)의 조합. `auth.js`의 `EF_ROLE.effective()`
  로직(base + '교직원' + granted[])을 서버 인가 로직으로 그대로 이식 가능.
- 서버관리자는 전 페이지 접근 가능(현재 `EF_ROLE.is('서버관리자', ...)`로 하드코딩된 최상위 규칙) — 이
  규칙은 유지 권장.

### 2.2 핵심 테이블 (요약)

localStorage 키 분석을 기반으로 도출한 최소 테이블 목록입니다. 정확한 컬럼은 섹션 4/6 참고.

- `organizations` (id, name, organization_type, neis_school_code, parent_id, active) — 학교·교육지원청 등 데이터 격리 단위
- `users` (id, email, password_hash, name, dept, rank, avatar_url, created_at)
- `organization_memberships` (user_id, organization_id, membership_type, active_from, active_to) — 사용자의 소속은 문자열 `org`가 아닌 이 테이블로 관리
- `role_requests` (id, user_id, requested_role, status, created_at)
- `user_roles` (user_id, role_code, scope_type, scope_id, granted_by, granted_at) — 승인된 역할과 학교·관할 범위 (`grantedRoles` 대체)
- `inquiries` (id, user_id, type, title, content, status, answer, answered_by, answered_at, created_at)
- `mentoring_requests` (id, requester_id, assigned_mentor_id, category, title, status, created_at, completed_at) — 현재 프론트의 단일 답변이 아닌 대화 스레드의 상위 엔터티
- `mentoring_messages` (id, request_id, sender_id, message_type, content, attachment_id, created_at) — `messages[]` 이관 대상
- `mentor_expertise` (mentor_id, category_code, scope_type, scope_id, active) — 분야별 다중 멘토 배정
- `mentor_slots` (id, mentor_id, starts_at, ends_at, status) 및 `appointments` (id, slot_id, request_id, mentee_id, platform, join_link, meeting_id, status)
- `consult_logs` (id, request_id, mentor_id, category, consulted_at, summary, action, status) — 상담일지·월별 출력의 원천 데이터
- `checklist_progress` (org, period, sector, category_id, item_no, result, action, checked_by, checked_at) —
  또는 `checklist_responses` 단일 행 테이블로 정규화 권장(현재는 org당 통째로 JSON 하나)
- `notices` (id, badge, title, date, views, author, body, created_by, created_at)
- `dispatches` (id, sender_id, title, description, due_at, dispatch_at, status, created_at)
- `dispatch_categories` (dispatch_id, category_code) 및 `dispatch_targets` (dispatch_id, target_type, target_id) — 수신 대상·분야를 정규화
- `dispatch_files` (id, dispatch_id, storage_key, filename, content_type, size, uploaded_at) — 현재 프론트의 다중 파일 시뮬레이션을 실제 객체 스토리지로 이관
- `mail_subscriptions` (user_id, category)
- `mail_bookmarks` (user_id, mail_id)
- `mail_reads` (user_id, mail_id, read_at) — 현재는 메일 자체의 전역 속성처럼 처리되어 사용자별 읽음 상태가 없음
- `personal_events` (id, user_id, title, date, memo) — 스마트공문달력의 개인 일정
- `home_widget_layouts` (user_id, widget_id, size, sort_order) — 홈 위젯 배치
- `saved_resources` (id, user_id, filename, size, saved_at) — 마이페이지 "저장한 자료" (실제 파일 스토리지 필요)
- `site_settings` (key, value) — 푸터 기관명/주소/저작권, 개인정보 담당부서/DPO 연락처 등 (현재
  서버관리자_푸터관리.html이 다루는 것 전부)
- `calendar_task_completions` (user_id, task_id, completed_at) — 스마트공문달력의 173개 정적 TASKS 완료 체크

### 2.3 파일 스토리지
다음 기능들은 전부 실제 파일 업로드/다운로드가 필요하지만 현재는 파일명만 저장하거나 완전히 가짜입니다:
- 업무발송 첨부파일(현재 프론트는 다중 파일·링크 메타데이터를 시뮬레이션하지만 실제 바이너리를 저장하지 않음)
- 마이페이지 "자료 추가하기"
- 서식 자동완성 마법사 최종 산출물(HWP/HWPX/PDF)
- 공문 OCR 업로드 원본
- 스마트 계획서 변환 업로드 원본
- 프로필 아바타(현재 base64 dataURL을 localStorage에 직접 저장 — 용량 제한 있음, 실제로는 S3/Object
  Storage + CDN URL로 교체 필요)

### 2.4 AI/외부 연동
> **2026-08-08**: AI 키가 없을 때 스마트공문달력은 호출 대신 서버 연동 필요와 수동 처리 경로를 안내한다.
- NEIS Open API: 학사일정 조회(스마트공문달력), 학교 검색 자동완성(회원가입/프로필) — 서버 프록시로 이전.
  참고로 NEIS 학교기본정보 API에는 학교장(교장) 이름 필드가 없음(개인정보라 공개 API 범주 밖) — 서식
  자동완성의 "기관장명" 자동 채우기는 NEIS로 해결 불가, 별도의 학교→교장 매핑 데이터가 있어야 함.
- LLM(현재 Gemini 자리): 계획서 초안, AI 코칭, 쪽지→일정 변환(스마트공문달력), AI 상담 챗봇 응답
  (AI업무어시스턴트_챗봇.html — 현재는 100% 정적 스크립트 응답), 공문 OCR 요약, 서식 자동완성 필드 채우기.
  전부 현재 미연동이거나 연출뿐이므로 백엔드에서 사실상 처음부터 설계. **특히 서식 자동완성의 "문서 본문을
  RAG 기반으로 실제 작성"은 사용자가 이 제품의 핵심 차별점으로 명시한 기능이니 섹션 4.3.1을 우선 참고.**
- 실시간성 요구: 업무우편함/1:1문의/멘토링/공지사항은 폴링이나 웹소켓 없이도 새로고침 기반으로 충분해
  보입니다(현재 UI에 "실시간 업데이트 중" 뱃지가 있는 화면이 일부 있으나 실제 로직은 없음 — 우선순위 낮음).

### 2.5 제품 가치와 구현 우선순위

- 핵심 흐름은 공문 배포가 아니라 **업무 수신 → 마감·서식 처리 → 멘토 상담 → 학교 점검·지원 현황**이다.
  업무배송과 인생도서관은 같은 사용자·학교·업무분야 체계 위에서 연결해야 한다.
- 1차는 공문 수신·확인·처리, 2차는 멘토 상담·학교 체크리스트, 3차는 RAG 기반 문서 초안·관리자 집계 순으로 구현한다.

---

## 3. 페이지 밖에서 알아야 할 추가 버그 (섹션 1에 안 들어간 것들)

### 3.1 서버관리자 대시보드류는 사실상 전부 정적 ✅ 2026-08-07 일부 실데이터 연동
서버관리자_대시보드/RAG데이터관리/리소스모니터링/보안설정, 학교관리자_대시보드/RAG데이터갱신/데이터리포트/
학교통합관리 8개 페이지의 인프라 관련 숫자·차트(CPU/메모리/트래픽 등)는 여전히 하드코딩/랜덤 시뮬레이션이며
백엔드 없이는 실측이 불가능합니다. 다만 2026-08-07에 **①실제로 쌓이는 localStorage 데이터로 대체 가능한
위젯은 실데이터로 교체**(서버관리자_대시보드의 "사이트 운영 현황" 5개 지표 + 최근 활동 피드는
roleRequests/ef_inquiries/ef_mentoring/ef_notices/ef_mentor_roster를 실제로 집계, 학교관리자_대시보드·
데이터 리포트·학교통합관리는 `ef_checklist_progress_<org>`로 "우리 학교 체크리스트 이행 현황"을 실제로
계산), **②IP allow/deny 리스트·MFA 토글·비밀번호 만료 정책(보안설정)을 실제 localStorage CRUD로 전환**
(`ef_security_ip_allowlist`/`ef_security_ip_blocklist`/`ef_security_mfa_enabled`/
`ef_security_pw_expiry_days`/`ef_security_concurrent_limit`), **③RAG 소스 재연결 상태를 저장**
(`ef_rag_source_status`), **④학교관리자_RAG데이터갱신은 업로드 UI 자체가 없던 빈 페이지였는데 드래그앤드롭
업로드 UI + 이력을 신규로 구성**(`ef_rag_school_uploads_<org>`, 파일명/크기만 저장, 실제 바이너리 스토리지
아님), **⑤나머지 죽은 버튼(새로고침/긴급정지/동기화/상세진단/pagination 등)은 최소한 `EF_MODAL`로 정직한
피드백**을 주도록 고쳤습니다. 실측 인프라 지표(CPU/메모리/트래픽/노드 상태)는 여전히 목업이므로 백엔드
설계 시 그 부분만 실제 모니터링 연동으로 교체하면 됩니다. (상세는 섹션 4.6)

### 3.2 서식 자료실 마법사는 단계 간 데이터가 유실됨 ✅ 2026-08-06 수정 완료 (sessionStorage로 연결)
서식 자동완성(서식선택 → 학교정보확인 → 생성완료)의 3단계는 각각 독립된 정적 페이지 이동(`location.href`)
일 뿐, 1단계에서 고른 템플릿이 2단계로, 2단계에서 입력한 필드가 3단계로 전달되지 않습니다. 실제로는
세션/쿼리스트링/서버 임시 저장 중 하나로 마법사 상태를 이어줘야 합니다.

### 3.3 업무발송 첨부파일 다중 업로드가 실제로는 1개만 저장됨 ✅ 2026-08-06 수정 완료
드롭존이 여러 파일을 선택하게 해주고 UI에는 칩으로 전부 보여주지만, 제출 시 코드는
`fileInput.files[0].name`만 읽습니다. 링크 첨부 칩도 DOM에만 추가될 뿐 제출 시 전혀 읽히지 않아 유실됩니다.
"업무 처리 시기"/"업무 발송 시기" 폼 필드도 제출 로직이 참조하지 않아 전부 무시되고 `due` 값은 항상
`'진행중'`으로 고정 저장됩니다.

### 3.4 통합대시보드류는 실제 데이터를 전혀 조회하지 않음 ✅ 2026-08-07 수정 완료
`인생도서관_장학사 및 업무담당자 통합 대시보드.html`은 KPI/차트/최근신청/월별 상담일지 출력까지 전부
하드코딩이었으나, 2026-08-07에 `EF_MENTORING`/`EF_INQUIRY`/`EF_MENTOR_ROSTER`/`EF_CONSULT_LOG`를 실제로
읽어 집계하도록 재작성했습니다(총 상담·이번달 증감·처리율·평균 처리시간·멘토 수·1:1 문의 처리율,
카테고리별 분포, 월별 추이, 최근 상담 신청 목록, 상담일지 인쇄 모달의 월 드롭다운·표 전부 실데이터).
데이터가 0건일 때도 깨지지 않고 "아직 데이터가 없습니다" 빈 상태를 보여줍니다. 백엔드 전환 시에는 이 집계
로직을 `GET /api/mentoring/stats` 같은 서버 집계 API로 옮기면 됩니다(현재 프론트가 집계하는 필드 목록이
곧 그 API가 반환해야 할 스키마 힌트가 됩니다). 만족도 점수(구 "평균 만족도 4.8/5.0")는 데이터 소스가
전혀 없어 제거했습니다 — 필요하면 별도 평가 수집 기능부터 설계해야 합니다.

### 3.5 멘토링에 "담당 멘토 배정" 개념 자체가 없음 ✅ 2026-08-06 수정 완료 (다중 멘토 + 카테고리 라우팅)
`ef_mentoring` 데이터 모델과 `인생도서관_멘토상담함.html`의 조회 로직 모두 특정 멘토에게 상담을 배정하는
개념이 없었습니다 — 로그인한 멘토가 누구든 전체 상담함을 봤습니다(2026-08-06 화상 상담 예약 기능도 같은
전제 위에 만들어져 슬롯이 전역 공유였음). 다음과 같이 다중 멘토 모델로 재설계했습니다:
- `EF_MENTOR_ROSTER`(신규, `ef_mentor_roster`) — 멘토 계정(email)별로 담당 업무분야(30개 태그 중
  1개 이상)를 배정. 관리는 신규 페이지 `서버관리자_멘토배정관리.html`에서.
- `공통_로그인.html`의 더미 계정에 멘토 2명(`test6@gbe.kr` 박서연 장학사, `test7@gbe.kr` 김도윤
  주무관) 추가 — 기존 `test3@gbe.kr`(이민수 수석교사)와 합쳐 총 3명, 각자 다른 카테고리 그룹 담당.
- `EF_MENTORING`(상담 요청)에 `mentorEmail`/`mentorName` 필드 추가 — 마이페이지에서 멘티가 업무분야를
  고르면 `EF_MENTOR_ROSTER.mentorsForCategory()`로 담당 멘토를 찾아 그 멘토에게 라우팅(담당 멘토가
  없으면 신청 자체를 막고 안내). `인생도서관_멘토상담함.html`은 이제 `EF_MENTORING.byMentorEmail(로그인
  계정)`으로 필터링해서 자기 담당 요청만 봄 — 실제로 test3/test6/test7 세 계정으로 각각 로그인해서
  서로의 요청이 안 보이는 것을 Playwright로 확인.
- `EF_APPOINTMENT`(화상 상담 슬롯/예약)에도 `mentorEmail`을 추가해 슬롯이 멘토별로 분리됨
  (`slots(mentorEmail)`/`openSlots(mentorEmail)` 등).
실서비스 전환 시 이 구조를 그대로 `mentoring_requests.assigned_mentor_id`/`mentor_slots.mentor_id`
컬럼으로 옮기면 됩니다. **남은 한계**: 한 카테고리에 멘토가 여럿이면 항상 배열의 첫 번째로만 라우팅(간단한
로드밸런싱/선택 UI는 없음), 계정은 여전히 로그인 페이지의 고정 더미 8명뿐(3.x/1.1 참고).

✅ **2026-08-07 추가 개선 2건 (같은 3.5/화상 상담 기능 범위):**
- **상담 신청 시점에 시간을 고르지 않도록 변경.** 이전엔 마이페이지의 "새 멘토 상담 신청" 모달에
  "화상 상담 예약(선택)" 슬롯 선택이 있었는데, 사용자가 "간단한 건은 대부분 대화로 끝날 텐데 처음부터
  화상회의를 강제할 필요는 없다"고 판단해 완전히 제거. 대신 멘티/멘토 양쪽의 대화창(`#mpThreadModal`,
  `#mentorAnswerModal`) 안에 "+ 상담 일정 잡기" 버튼을 두어, 필요할 때만 접혀 있던 폼을 펼쳐 예약하는
  방식으로 바뀜(`maApptFormExpanded`/`mpApptFormExpanded` 상태로 토글). 예약이 이미 있으면 토글 대신
  바로 예약 정보/취소 버튼을 보여줌.
- **"매주 반복" 슬롯 등록을 요일+반복횟수("몇 주간") 대신 요일+날짜 범위(시작일~종료일)로 변경**
  (`EF_APPOINTMENT.addRecurringSlotsRange(mentorEmail, weekday, time, startDate, endDate)`가
  기존 `addRecurringSlots(...weeksAhead)`를 완전히 대체). 출장/수업 등으로 날마다 상담 가능 여부나
  시간이 달라질 수 있다는 점을 고려해, 일괄 생성 후에도 "상담 가능 시간 설정" 목록에서 슬롯 하나하나를
  인라인으로 수정(`EF_APPOINTMENT.updateSlot(id, date, time)`, 예약된 슬롯은 수정 불가)하거나 삭제할
  수 있게 함.

---

## 4. 섹션별 페이지 레퍼런스

각 페이지의 "목적 / 현재 데이터 소스 / 필요한 백엔드" 요약입니다. 더 상세한 라인 단위 근거가 필요하면
해당 파일을 직접 열어 이 문서의 힌트(함수명/변수명)로 검색하면 빠르게 찾을 수 있습니다.

### 4.1 업무 배송

**업무배송_스마트공문달력.html** (5,174줄, 최대 파일)
학사일정(NEIS 실시간) + 연간 업무배송 캘린더(정적 TASKS 173건) + 개인 일정 + AI 도우미 통합 달력.
`TASKS`의 마감일은 실제 날짜가 아니라 알고리즘으로 요일에 분산 배치한 값(개발자가 주석으로 "서버 구현 시
실제 배송일로 대체" 명시). Google/Outlook 캘린더 "연동"은 완전 가짜(계정명 하드코딩,
`syncNow()`는 스피너만). 필요 테이블: `personal_events`, `calendar_task_completions`,
`calendar_connections`(실제 OAuth2 구현 시). NEIS/AI는 서버 프록시로.

**업무배송_업무우편함.html**
구독 태그(30개 업무분야) 기반 수신함 + 책갈피로 개인 달력 표시. `ef_mail_data`를 업무발송.html과
공유하지만 1.1 버그로 실제 수신되는 메일은 거의 없고, 최초 12건 시드 데이터만 보임. 상세 모달 "서식
다운로드" 버튼은 완전히 죽어있음. XSS 이슈 있음(1.7).

**업무배송_업무발송.html**
업무배송 담당자 전용 발송 화면. 3.3의 다중파일/링크/시기 필드 유실 버그 있음. `dispatches` +
`dispatch_files`(+ `dispatch_categories`) 테이블 필요.

**업무배송_맞춤형업무가이드.html** *(이번 세션에 신규 구현)*
"오늘의 필수 업무" 우선순위 목록 + 이번 주 학사일정 + AI 추천 배너로 구성된 대시보드. 현재는 전부
하드코딩 예시 데이터입니다. 실제로는 로그인 사용자의 부서/역할 + 스마트공문달력의 학사일정을 기반으로
마감 임박 업무를 우선순위 정렬해 보여주는 추천 로직이 필요합니다(현재 `TASKS`/학사일정과 연결되어 있지
않음 — 신규 구현 시 함께 설계 권장).

### 4.2 인생 도서관

**인생도서관_업무체크리스트.html**
학사/회계 2개 분야 × 상반기/하반기, 대분류로 그룹핑된 자가진단 문항(품질검증 완료, 문항 데이터 자체는
개인정보 없는 공통 문항 은행). 항목별 이행(○)/미흡(△)/미이행(×) + 조치사항 텍스트를 체크할 때마다
로그인 계정과 날짜가 자동으로 배지에 기록됩니다(실명 수기 입력 없음). `ef_checklist_progress_<org>` 키
하나에 JSON 통째로 저장 — 같은 기관 소속이면 브라우저 로컬에서는 공유된 것처럼 보이지만 **실제로는
사용자 브라우저별로 독립**이라 다른 사람 화면에는 반영되지 않습니다. 실제 "같은 학교 구성원이 함께
체크"를 구현하려면 `checklist_responses(org_id, period, sector, category_id, item_no, result, action,
checked_by, checked_at)` 형태의 서버 테이블이 반드시 필요합니다. 한글(HWPX) 다운로드는 클라이언트에서
JSZip으로 base64 템플릿의 `[[문항번호]]`/`[[조치코드]]` 자리표시자를 치환하는 방식으로 완전히
동작합니다(서버 불필요, 순수 프론트 기능).

**AI업무어시스턴트_챗봇.html**
UI는 카테고리 선택 → 채팅 → AI 응답 흐름을 갖췄지만, **AI 응답은 100% 사전 작성된 고정 문자열**입니다
(`setTimeout` 후 동일한 안내 문구 출력). 실제로는 `POST /api/chat`(RAG 기반 LLM 응답) 엔드포인트가
필요합니다. 파일 첨부 UI(여러 확장자)는 이번 세션에 추가했지만 **첨부파일을 실제로 전송/분석하는 백엔드는
없음** — 현재는 파일명만 말풍선에 표시. "새 대화 시작"의 "최근 대화 내역" 목록도 정적입니다. 대화 기록을
영속화하려면 `chat_sessions`/`chat_messages` 테이블 필요.

**인생도서관_마이페이지.html**
멘토 찾기·신청 + 예약된 상담 + 저장한 자료(파일 바이너리 없이 파일명만 저장) + 상담 신청.
"예약된 상담" 카드와 신청 모달의 화상 상담 슬롯 선택은 ✅ 2026-08-06에 `assets/js/mentoring.js`의
`EF_APPOINTMENT`(멘토상담함과 공유하는 `ef_mentor_slots`/`ef_appointments`)로 다시 구현되어, 더 이상
페이지 단독의 하드코딩이 아니라 멘토가 실제로 열어둔 슬롯을 예약하는 흐름입니다(프론트+localStorage
시뮬레이션 — 실제 Zoom/Teams 연동은 없음). 멘토가 예약에 Zoom/Teams/기타 중 하나를 골라 입장 링크
또는 회의번호+비밀번호를 등록하면(아래 멘토상담함 항목 참고) 이 페이지의 "예약된 상담" 카드에도 실제
클릭 가능한 링크(http/https만 허용) 또는 회의번호/비밀번호 텍스트로 그대로 반영됩니다 — "회의실 입장"
토스트는 제거하고 실제 표시로 교체했습니다. ✅ `submitMentorRequest()`가 로그인 사용자와 무관하게
이름/학교를 하드코딩하던 버그도 2026-08-06에 수정(이제 이메일 아이디 부분과 `org_이메일` 값에서
파생 — profile-modal.js와 동일한 규칙). ✅ 같은 날 상담 목록 테이블(`renderMyRequests`)이 제목/내용을
`innerHTML`에 그대로 이어붙이던 저장형 XSS도 `escapeHtml()`로 수정(1.7과 같은 버그 패턴). ✅ 같은 날
후속 요청으로 (1) 업무분야 선택 시 3.5의 `EF_MENTOR_ROSTER`로 담당 멘토를 찾아 표시하고 그 멘토의
슬롯만 보여주도록 변경, (2) 상담이 "쪽지 1건-답변 1건"이 아니라 계속 이어지는 대화 스레드
(`EF_MENTORING.messages[]`)가 되도록 재구성 — 이전엔 마이페이지에 멘토 답변을 보거나 답장할 방법이
전혀 없었는데, 신규 `#mpThreadModal`(대화 보기)에서 메시지 주고받기 + 파일 첨부(파일명만, 실제 업로드
없음 — AI챗봇 파일첨부 UI와 같은 수준)가 가능해짐.

**인생도서관_멘토상담함.html**
멘토 전용 상담 응답함. ✅ "월별 상담일지 출력" 모달은 인쇄 레이아웃만 있고 실제 월별 집계 로직이
없었으나 2026-08-06에 `EF_CONSULT_LOG` 연동으로 해결됨(하단 설명 참고).
"상담 가능 시간 설정"(새 모달), 우측 "예약된 상담"/"최근 답변 완료" 카드, 상담 요청 답변 모달 안의
화상 상담 예약 섹션은 ✅ 2026-08-06에 `EF_APPOINTMENT`/`EF_MENTORING` 실데이터 기반으로 재구현됨(위
마이페이지 항목 참고). ✅ 같은 날 예약 시 Zoom/Teams/기타 화상회의 플랫폼을 고르고 입장 링크(있으면
비밀번호 불필요) 또는 회의번호+비밀번호를 입력하는 폼을 답변 모달에 추가 — 멘티가 슬롯만 먼저 예약한
경우(마이페이지에서 예약) 멘토가 나중에 이 정보를 채워 넣을 수 있는 별도 폼도 있음. 링크는
`isSafeHttpUrl()`로 http/https만 허용(그 외, 예: `javascript:` 스킴은 클릭 가능한 링크로 만들지
않음)하고 DOM `.href` 프로퍼티로만 설정, 회의번호/비밀번호는 `textContent`로만 표시해서 멘토가 입력한
값이 HTML로 해석되지 않게 함. ✅ 상담 요청 테이블/최근 답변 완료 목록도 같은 날 `escapeHtml()`로 XSS
수정(1.7과 같은 버그 패턴, `renderMentorInbox`/`renderRecentAnswered`). 여전히 진짜 화상회의 서버
연동(계정 생성, 실시간 참여자 수 등)은 없고 순수 프론트 시뮬레이션. ✅ 같은 날 3.5에서 멘토 1인
모델(슬롯 전역 공유)을 다중 멘토 + 카테고리 라우팅으로 교체 — 로그인한 멘토는
`EF_MENTORING.byMentorEmail()`로 자기 담당 요청만 보고, 슬롯도 `EF_APPOINTMENT.slots(멘토이메일)`로
분리됨. ✅ 같은 날 "상담 가능 시간 설정" 모달에 요일 반복 등록(`addRecurringSlots` — 요일+시간을
고르면 앞으로 N주치 날짜를 한 번에 생성) 추가, 답변 모달이 대화 스레드로 바뀌면서 "완료 처리"/"다시
열기" 토글과 "상담 일지" 작성 폼(`EF_CONSULT_LOG`, 신규)이 추가됨 — 상담 일지는 "월별 상담일지
출력" 인쇄 모달과 실제로 연동되어(로그인 멘토 + 선택한 달 기준으로 필터링) 더 이상 하드코딩된 3줄이
아님(작성자 이름도 `EF_MENTOR_ROSTER`에서 가져옴).

**인생도서관_장학사 및 업무담당자 통합 대시보드.html** *(이번 세션에 접근권을 장학사 전용으로 수정)*
전체가 정적 KPI/차트(총 상담 128건, 처리완료율 92% 등)입니다. 실제 `ef_mentoring`/`ef_inquiries`
데이터를 집계해서 보여주는 통계 API가 필요합니다(3.4 참고).

### 4.3 서식 자료실

6개 페이지(통합검색 / 서식선택 / 학교정보확인 / 생성완료 / 공문OCR요약 / 스마트계획서변환) 중 검색·폼 연결·
미리보기는 프론트에서 동작하지만, 실제 OCR/AI 문서 생성은 아직 없다. 체크리스트 페이지의 내장 HWPX 템플릿
치환만 실제 HWPX 산출물이며, 생성완료·스마트계획서는 HTML 미리보기로 명확히 표시한다.
공통적으로 필요한 백엔드: 서식 템플릿 저장소(`templates` 테이블 + 실제 HWP/HWPX 바이너리 스토리지), 문서
생성 서비스(템플릿 + 사용자 입력값 병합 → 실제 파일 산출), OCR/문서이해 LLM 파이프라인, 검색 엔진(키워드 +
벡터 검색). ~~마법사 3단계는 상태 연결이 끊겨 있음~~ → 2026-08-06 sessionStorage로 연결 완료(3.2 참고,
템플릿 선택·입력 필드는 이어지지만 실제 문서 생성/OCR/변환 자체는 여전히 미구현).

✅ **2026-08-07 추가 개선**: 실제 AI/OCR/검색 엔진 없이도 프론트에서 만들 수 있는 부분은 실동작으로
전환했습니다 — 통합검색의 키워드/카테고리/부서/태그 필터·정렬·페이지네이션이 화면에 있는 카드를 실제로
필터링/정렬하고, STEP1(서식선택)의 카테고리 칩이 실제로 카드를 걸러내며 "미리보기"가 실제 모달을 띄우고,
STEP2(학교정보확인)의 "초기화"/확대축소가 실제로 폼과 미리보기 스케일을 조작하고, STEP3(생성완료)와
스마트계획서변환의 "다운로드"는 마법사에서 모은 입력값을 채운 실제 HTML 문서를 Blob으로 생성해
다운로드합니다(진짜 HWP 바이너리는 아니지만 "클릭해도 아무 파일도 안 생김"보다 훨씬 나은 상태). 공문OCR
요약은 업로드한 파일명이 결과 화면에 실제로 반영되도록 했고, 필터/재시도/상세보기/FAB도 최소한의 실제
UI 상호작용을 갖췄습니다. 여전히 검색 결과·OCR 요약·변환 결과 자체의 "내용"은 고정 목업입니다(진짜
검색엔진/OCR/LLM이 있어야 하는 부분).

### 4.3.1 ⚠️ 우선순위 높음 — 서식 자동완성의 진짜 킬포인트는 "문서 본문까지 AI가 초안 작성"이며, 아직 없음

**2026-08-08, 사용자 판단**: 서식 자동완성 마법사(STEP1~3)가 "선생님이 HWP 파일을 직접 열어서 학교명·날짜
고치는 것"보다 실제로 더 빠르고 편해야 이 기능을 쓸 이유가 생기는데, 지금 있는 수준(계정 정보로 학교명 등
메타데이터 몇 개만 자동 채우는 것 — 아래 참고)은 그 기준에 못 미친다고 명시적으로 판단했습니다
("단순 날짜·제목·학교장 정도만 바뀌는 건 너무 낮은 수준이고 굳이 우리가 구현하지 않아도 될 정도"). 진짜
필요한 건 **문서 본문 내용 자체를 AI가 (RAG 데이터 기반으로) 초안 작성**해주는 것이고, 이건 실제 LLM 백엔드
없이는 구현 불가능해 이번 세션에서는 보류하고 여기에 요구사항만 정리해 남깁니다(그럴듯한 목업조차 만들지
않기로 함 — 이 기능은 프로토타입의 다른 장식 버튼들과 달리 "제품 존재 이유"에 가까운 핵심 기능이라, 어설픈
가짜보다는 실제 구현을 기다리는 게 낫다는 판단).

**현재 상태(무엇이 진짜고 무엇이 목업인지)**: `서식 자료실_학교정보확인.html`(STEP2)의 헤더는 "AI가 기존
RAG 데이터와 달력을 바탕으로 문서 초안을 작성했습니다"라고 써 있지만, 실제로 하는 일은 5개 입력 필드
(`wizSchoolName`/`wizPrincipalName`/`wizMeetingDate`/`wizMeetingPlace`/`wizMeetingAgenda`, 스크립트 내
`WIZARD_DEFAULT_FIELDS`)를 고정 더미값 또는(2026-08-08부터) 로그인 계정의 소속·직위 정보로 채우는 것뿐입니다.
"주요 안건 요약"(`wizMeetingAgenda`)조차 완전히 고정된 텍스트("1. 2026학년도 1학기 방과후학교 운영
계획(안)..." 등)이고, 사용자가 무엇을 입력하든 AI가 실제로 작성한 문장은 하나도 없습니다.

**실제로 필요한 것**:
- **입력**: STEP1에서 고른 템플릿 종류 + 사용자가 짧게 입력하는 안건/목적 키워드(예: "이번 학운위 안건:
  방과후 예산안, 급식 소위원회 구성") + 로그인 계정의 학교/직위 컨텍스트.
- **RAG 소스**: 이 사이트가 이미 실제로 쌓고 있는 데이터를 우선 활용 — 업무배송 공문(`ef_mail_data`,
  백엔드 전환 시 `dispatches` 테이블), 인생도서관 상담·상담일지(`ef_mentoring`/`ef_consult_logs`), 학교
  업무 체크리스트(`ef_checklist_progress_<org>`) 등. 여기에 더해 학교별 과거 작성 문서 아카이브가 있다면
  더 좋음(현재는 없음 — `saved_resources`/`templates` 테이블 설계 시 함께 고려).
- **출력**: 단순 필드 치환이 아니라, "주요 안건" 같은 본문 섹션에 **실제로 생성된 문단**이 채워져야 함
  (사용자는 그 초안을 다듬기만 하면 되는 수준). 지금 STEP2의 "주요 안건 요약" textarea가 이 최종 결과물을
  받는 자리가 되어야 합니다.
- **아키텍처**: 반드시 서버 프록시를 통한 LLM 호출이어야 함(섹션 1.6/2.4와 같은 이유 — 클라이언트에 API 키
  노출 금지). `POST /api/documents/draft` 같은 엔드포인트로, 템플릿ID + 사용자 프롬프트 + (필요시) RAG
  컨텍스트 문서 ID들을 받아 초안 텍스트를 반환하는 형태를 권장.
- **우선순위**: 서식 자동완성 마법사 전체의 존재 이유가 이 기능에 달려 있다고 봐도 됨 — 백엔드 개발
  착수 시 다른 장식적 AI 연출(공문OCR요약, 스마트계획서변환 등)보다 먼저 검토할 가치가 있음.


### 4.3.2 2026-08-15 결정 — 업로드형보다 열린 문서 직접 편집을 우선

제품의 주 문서 경험은 업로드·다운로드가 아니라 로컬 에이전트가 현재 활성 HWP/HWPX/ODT를 읽고 계획을 보여준 뒤 승인된 수정만 적용하는 방식으로 잡는다. 브라우저만으로 데스크톱 한글을 제어할 수 없으므로 Windows 로컬 에이전트와 한컴 Automation PoC가 선행되어야 한다. 상세 아키텍처, `DocumentPatch`, 충돌 방지, 승인·백업·감사 기준은 `DOCUMENT_AGENT_SPEC.md`를 따른다.

이 결정에 따라 문서 에이전트 기술 검증은 기존의 후순위 문서 AI가 아니라 인증·파일 저장소와 병행할 1단계 과제로 올린다. 서버 HWP SDK/WebHWP는 보조 경로로 비교한다.

**프론트에서 확정한 기능 계약**: 계획 항목 체크, 근거·위험·보존 조건, 변경별 수락·거절, 결정 완료 전 적용
차단, `DocumentPatch`와 `DocumentApplyReceipt` JSON 다운로드까지 구현했다. 백엔드는 새 화면을 만들지
말고 다음 값을 실제 응답으로 교체한다.

1. `POST /api/document-agent/plan`에서 근거·위험·보존 정책이 포함된 패치를 반환한다.
2. 사용자가 수락한 `operationIds[]`에 대해서만 적용 토큰을 발급한다.
3. 로컬 에이전트가 백업·리비전·패키지·한글 재열기 결과를 영수증으로 반환한다.
4. 실행하지 않은 검사는 `passed`가 아니라 `not_performed`로 둔다.
5. 서버 감사 로그에는 본문과 전체 로컬 경로를 제거한 영수증만 저장한다.

외부 엔진 후보와 라이선스 경계는 `DOCUMENT_AGENT_SPEC.md` 14~15절을 필수로 따른다. 특히
Docufinder는 현재 비프로덕션 평가 전용이고, Canine89 및 jkf87의 라이선스 미확인 코드는 제품과 개발환경에
복사하지 않는다.

**인계 시 빠른 라이선스 판정표** — 아래 판정은 코드 도입 범위에 대한 보수적 개발 기준이며 법률 자문이 아니다.

| 구분 | 저장소/기술 | 백엔드 개발 규칙 |
|---|---|---|
| 도입 검토 가능 | `edwardkim/rhwp`, `DoHyun468/claw-hwp`, `chrisryugj/kordoc` (MIT) | 저작권·라이선스 고지 보존, 실제 반입 버전 고정 |
| 도입 검토 가능 | `airmang/python-hwpx`, `hancom-io/hwpx-owpml-model` (Apache-2.0) | LICENSE/NOTICE 의무 확인, 수정·배포 내역과 SBOM 기록 |
| 조건부·현재 반입 금지 | `neolord0/hwpxlib` | README/POM 표시는 Apache-2.0이나 루트 LICENSE가 확인되지 않아 정확한 배포 아티팩트의 라이선스 확인 전 vendoring 금지 |
| 비프로덕션 평가만 | `chrisryugj/Docufinder` (BSL 1.1) | Change Date 2030-04-15 전 프로덕션·서비스 코드에 포함하지 않는다. 비상업이라는 이유만으로 허용 범위를 넓히지 않는다. |
| 개념 참고만·코드 사용 금지 | `Canine89/hwpxskill`, `jkf87/hwpx-skill`, `jkf87/hwp2hwpx-python-refactor`, 로컬 `hwpx-skill-fixed.zip` | 루트 라이선스 부재 또는 라이선스 검토 중이므로 복사·설치·번들·파생 코드 작성 금지. 공개 동작 아이디어만 독립 구현한다. |
| 별도 계약 검토 | 한컴 Automation, WebHwp, HWP SDK | 오픈소스 저장소 라이선스와 분리한다. 현재 비상업 개인 PoC 범위를 넘기기 전 한컴의 최신 약관·승인 범위를 다시 확인한다. |

코드를 반입할 때는 커밋 SHA/버전, 원문 LICENSE와 NOTICE, 수정 파일, 직접/전이 의존성, 배포 형태를
`THIRD_PARTY_NOTICES.md`와 SBOM에 기록한다. 현재 실제 반입한 `rhwp` 식별값과 MIT 전문도 이 파일에
기록되어 있다. “GitHub에서 공개됨”, “무료”, “개인 개발”은 라이선스 증거로
사용하지 않는다. 상세 근거와 엔진별 역할은 `DOCUMENT_AGENT_SPEC.md` 14~15절이 단일 기준이다.

### 4.4 안내센터

**안내센터_공지사항.html / 안내센터_공지사항_상세.html** *(이번 세션에 상세 페이지를 범용화)*
목록 4건(빌트인, 하드코딩) + 서버관리자가 작성한 공지(`ef_notices`, 실제 CRUD 동작). 상세 페이지는
`?id=` 쿼리로 같은 데이터를 조회하는 구조로 통일했습니다. 백엔드 연동 시 `notices` 테이블 하나로 흡수하면
됨(빌트인 4건은 시드 데이터로 이관).

**안내센터_시스템 안내.html**
매뉴얼 성격 랜딩 페이지(이번 세션에 미동작 검색창 제거). 실제 검색/문서 콘텐츠 관리가 필요하면 별도
CMS/문서 테이블 설계 필요(현재는 카드 링크 몇 개뿐).

**안내센터_1대1문의관리.html / 안내센터_시스템 안내.html**
문의 등록 UI는 `안내센터_시스템 안내.html`에 있고, 관리 화면과 같은 `ef_inquiries`를 사용한다.
`EF_INQUIRY.answer()`는 `answeredBy`와 `answeredAt`을 기록한다. 백엔드는 `inquiries`, `inquiry_messages` 또는
답변 필드로 이전하고, 작성자·답변자·조직 권한과 서버 시각을 강제한다.

### 4.5 공통 인증/프로필/법적고지

로그인/회원가입/프로필수정 + 4개 법적고지 페이지. 1.8, 1.9에서 다룬 인증 관련 문제 외에, 4개 법적고지
페이지(개인정보처리방침/이용약관/이메일무단수집거부/찾아오시는길)는 하단에 동일한 가짜 CMS 패턴을 공유
(`ef_footer_*`, `ef_contact_*`, `ef_dpo_*` — 서버관리자_푸터관리.html에서만 편집 가능, **브라우저별로만
저장되어 다른 사용자/기기에는 반영 안 됨**). `site_settings` 테이블로 이전 시 전 사용자에게 일관되게 반영.

### 4.6 관리자 (서버관리자 8 + 학교관리자 4)

**실제 데이터 연동이 있는 것**: 서버관리자_푸터관리.html(완전 동작), 서버관리자_역할관리.html(동작하나
1.2/1.3의 스키마 정리 필요), 서버관리자_회원관리.html(1.1/1.4의 버그로 사실상 미동작),
서버관리자_멘토배정관리.html *(2026-08-06 신규)*(완전 동작 — `EF_MENTOR_ROSTER`로 멘토 계정별 담당
업무분야를 배정하고, 마이페이지/멘토상담함이 실제로 이 데이터로 라우팅함. 3.5 참고).

**나머지 8개는 인프라 지표만 정적 목업, 그 외는 2026-08-07에 부분 실동작으로 전환**:
- 서버관리자_대시보드 — CPU/트래픽 차트는 여전히 목업이지만, "사이트 운영 현황"(역할 승인 대기/1:1 문의
  미답변/멘토링 진행중/공지사항 수/배정 멘토 수 + 최근 활동 피드)은 실제 localStorage 집계.
- RAG데이터관리 — 문서 업로드 진행률바는 여전히 가짜 연출이지만, 검색창은 실제로 소스 목록을 필터링하고
  "Reconnect"는 실제로 상태를 바꿔 `ef_rag_source_status`에 저장(새로고침해도 유지).
- 리소스 모니터링 — CPU/메모리 등은 여전히 `Math.random()` 기반 가짜 실시간(백엔드 필요 영역). 검색은
  실제 노드 필터링, `more_vert` 메뉴는 `EF_MODAL.confirm`으로 정직한 피드백.
- 보안설정 — IP allow/deny list, MFA 토글, 비밀번호 만료 정책, 동시 접속 세션 제한이 전부 실제
  localStorage CRUD로 전환(`ef_security_ip_allowlist`/`ef_security_ip_blocklist`/`ef_security_mfa_enabled`/
  `ef_security_pw_expiry_days`/`ef_security_concurrent_limit`). "전체 로그 내보내기"는 실제 CSV 다운로드.
  단, 이 값들은 여전히 프론트에서만 유효하며 실제 서버 보안 정책에는 반영되지 않음 — 실배포 시 반드시
  서버 측 정책으로 재구현 필요.
- 학교관리자_대시보드/데이터 리포트/학교통합관리 — 인력배분·트래픽 등은 목업이지만, 세 페이지 모두
  `ef_checklist_progress_<org>`를 실제로 읽어 "우리 학교 체크리스트 이행 현황"(O/S/X/미체크 집계,
  상반기·하반기)을 보여줍니다. 데이터 리포트의 "Excel 다운로드"는 화면에 보이는 표를 실제 CSV로,
  "PDF 리포트 출력"은 `window.print()`로 실제 인쇄. 학교통합관리의 "제안 적용하기"는 확인 후 실제로 화면의
  업무량 바를 이동시키고 "적용 완료"로 잠금 처리(실제 인사 반영은 백엔드 필요, 모달로 안내). "보고서 출력"은
  현재 검색 필터가 적용된 인원 표를 실제 CSV로 내보냄.
- 학교관리자_RAG데이터갱신 — 원래 업로드 UI 자체가 없는 빈 페이지였던 것을 신규로 구성(드래그앤드롭 +
  업로드 이력이 `ef_rag_school_uploads_<org>`에 저장·복원됨, 파일명/크기만 — 바이너리 스토리지는 아님).

여전히 CPU/메모리/트래픽/실시간 노드 상태 같은 **진짜 인프라 지표**는 백엔드(실제 모니터링 시스템 연동)
없이는 불가능하므로 목업으로 남아 있습니다. 백엔드 설계 시 이 부분만 실제 쿼리/모니터링 연동으로 교체하면
됩니다. 특히 보안설정.html은 실제로 구현되면 서버 보안 정책(IP allowlist 등)에 직결되므로 우선순위 높게
검토 필요.

### 4.7 홈 (index.html) *(위젯 시스템 신규 구현 및 히어로 배너 위젯화)*

기존 상단 히어로 배너를 3개 위젯(**메인 배너**, **통합 검색**, **실시간 업무 처리 현황**)으로 분리하여, 하단 6개 위젯(업무배송 캘린더, 인생도서관, 한글파일뚝딱, 서식자료실, 업무체크리스트 진행률, 최근 공지사항)과 동일하게 총 9개 위젯 전체를 추가·삭제·순서변경·크기조절 가능하도록 개편했습니다 (홈 화면 전체가 단일 위젯 캔버스화됨).

- **편집 진입점 통일**: 히어로 내부의 중복된 "화면 구성하기" 버튼과 "내 홈 화면" 소제목을 정리하고 상단 "화면 편집" 버튼 하나로 진입점을 통일했습니다.
- **카드 스타일 강화**: 기존 반투명/유리효과 위젯을 밝은 그리드 백그라운드에서도 가독성이 유지되도록 불투명 카드 스타일로 다시 래핑했습니다.
- **데이터 바인딩 및 백엔드 연동 필요 사항**:
  - **위젯 배치 저장**: 사용자별 위젯 선택/순서/크기는 `ef_home_widgets_<email>`에 저장되며, 백엔드 연동 시 `home_widget_layouts` 테이블로 이전 필요.
  - **실시간 업무 처리 현황 & 통합 검색**: 위젯 영역으로 분리 및 UI 래핑까지만 완료된 상태입니다. 검색 로직(백엔드 통합 검색 API) 및 실시간 현황 수치(87% 완료, 기안 12건, AI 분석 34건 등)는 고정 정적 목업이며, 백엔드 구축 시 실제 업무 처리 데이터 및 검색 엔드포인트와 연결이 필요합니다.
  - **기타 위젯 콘텐츠**: 업무체크리스트 진행률 위젯만 실제 `ef_checklist_progress_<org>` 데이터를 읽어 오며, 나머지 위젯(캘린더, 공지사항, 서식 등) 역시 백엔드 연동 2단계에서 각 영역별 실제 API 데이터로 교체해야 합니다.

---

## 5. 알려진 미구현/장식 기능 총정리

빠르게 스캔할 수 있도록 "버튼은 있는데 아무 일도 안 일어나는 것" 위주로 모았습니다(위 섹션에서 이미 다룬
것 포함, 중복 감수하고 한곳에 모음).

- ~~로그인: "비밀번호 찾기", "교육기관 인증서로 로그인" — 죽은 링크/버튼~~ ✅ 2026-08-07 수정 완료
  (클릭 시 `EF_MODAL.alert`로 "준비 중" 정직 안내)
- 회원가입: 이메일 인증 PIN이 6자리 숫자면 무엇이든 통과(실제 대조 없음) — 백엔드 필요, 그대로 남김
- 프로필수정.html: 전체가 도달 불가능(사실상 profile-modal.js가 대체) + "변경사항 저장" 버튼 무반응.
  (단, 실제로 쓰이는 profile-modal.js 쪽 모달의 저장 버튼은 2026-08-06에 소속/직위를 실제로 저장하도록
  고쳤습니다 - 1.9 참고. 여기 적힌 건 아무도 도달할 수 없는 이 정적 파일 자체의 죽은 버튼 얘기입니다.)
- ~~업무우편함: 상세 모달 "서식 다운로드" 무반응~~ ✅ 2026-08-07 수정 완료 (마감 안내 텍스트 파일을 실제로
  다운로드 — 진짜 hwp 원본은 백엔드 파일 스토리지 필요)
- ~~업무발송: 다중파일 2번째부터 유실, 링크첨부 유실, 발송시기/처리시기 선택 무시됨~~ ✅ 2026-08-06 수정 완료
- 스마트공문달력: Google/Outlook 연동 100% 가짜(백엔드 필요, 그대로 남김). ~~CSV 내보내기 코드 있으나
  주석 처리되어 비활성~~ ✅ 2026-08-07 수정 완료(원래 완성돼 있던 코드가 통째로 주석 처리돼 있었던 것 —
  주석만 해제)
- ~~서식 통합검색: 검색/필터/정렬/페이지네이션 전부 무반응~~ ✅ 2026-08-07 수정 완료(화면에 있는 카드
  기준 클라이언트 필터링/정렬/페이지네이션 — 검색 결과 "내용" 자체는 여전히 목업)
- ~~서식선택(STEP1): 카테고리 필터/탭/미리보기/업로드 전부 무반응~~ ✅ 2026-08-07 수정 완료. (카드 6개가
  어떤 서식인지 구분 없이 전부 같은 곳으로 이동하던 것은 2026-08-06에 수정 - 이제 고른 서식명이 STEP2/3까지
  이어집니다.)
- ~~학교정보확인(STEP2): "초기화"/확대축소 무반응~~ ✅ 2026-08-07 수정 완료. (~~마법사 단계 간 입력값
  유실~~ 은 2026-08-06 수정 완료)
- 생성완료(STEP3): 입력값을 채운 HTML 미리보기를 다운로드한다. ✅ 2026-08-15 실제 형식에 맞게 파일명·버튼을
  `.html`로 표시하고 HWPX 버튼은 백엔드 연동 전까지 비활성화했다.
- ~~공문OCR요약: 업로드 내용과 무관하게 고정 요약 출력, 필터/재시도/상세보기/FAB 무반응~~ ✅ 2026-08-07
  수정 완료(업로드 파일명은 실제 반영, 필터/재시도/상세보기/FAB 실동작 — 요약 "내용" 자체는 여전히 목업)
- 스마트계획서변환: 업로드 파일 본문은 읽지 않고 파일명과 사용자가 입력한 값으로 HTML 미리보기를 만든다.
  ✅ 2026-08-15 실제 HWP 분석처럼 보이던 진행 문구와 `.hwp` 다운로드 표기를 정직한 프로토타입 안내로 수정했다.
- ~~마이페이지: "다운로드"/"회의실 입장하기" 토스트만 뜸~~ 화상상담 회의 정보는 2026-08-06에 이미 실제
  표시로 교체됨(1.5 위 3.5 참고). "저장한 자료"는 실제로 localStorage에 저장/삭제되는 기능이었으나
  2026-08-07에 파일명 저장형 XSS(이스케이프 누락)를 발견해 수정함.
- ~~멘토상담함: "상담 가능 시간 설정" 완전 죽은 버튼~~ ✅ 2026-08-06 수정 완료 (localStorage 기반 슬롯/예약 시스템)
- ~~통합대시보드: "편람 업데이트 바로가기"/"전체보기"/"신청 목록 전체 보기" 죽은 링크~~ ✅ 2026-08-07
  수정 완료(3.4 참고 — "전체보기"류는 실제 목록 펼치기/접기 토글로, "편람 업데이트"는 정직한 준비중 안내로)
- ~~안내센터_1대1문의관리: 답변자/답변일 미기록~~ ✅ 2026-08-07 수정 완료(`EF_INQUIRY.answer()`가
  `answeredBy`/`answeredAt`을 자동 기록, 관리 화면과 신청자 화면 양쪽에 표시)
- 안내센터_시스템 안내: (이번 세션에 미동작 검색창 제거 완료)
- ~~서버관리자 8개 + 학교관리자 4개 대시보드류: 대부분의 버튼/토글이 `console.log` 또는 무반응~~
  ✅ 2026-08-07 대부분 수정 완료 — 상세는 섹션 3.1/4.6 참고(실측 불가능한 인프라 지표만 목업으로 남음)
- ~~`main.html`, `main2.html`: 어디서도 링크되지 않는 고아 파일~~, ~~배치 수정용 스크래치 스크립트
  20여 개(add_btn.js, fix_sidebar.js, temp_script_*.js 등)~~, ~~`school-self-check-temp/`~~
  ✅ 2026-08-07 전부 삭제 완료.

---

## 6. localStorage 키 전체 인벤토리 (부록)

| 키 | 모양 | 사용처 |
|---|---|---|
| `currentUser` | 문자열(이메일) | 전역(auth.js) |
| `currentRole` | 문자열(한글 역할명) | 전역(auth.js) |
| `grantedRoles_<email>` | 배열 | auth.js, profile-modal.js, 서버관리자_역할관리.html |
| `roleRequests` | 배열 `[{email, requestedRole}]` | 회원가입, profile-modal.js, 역할관리.html |
| `org_<email>`, `rank_<email>` | 문자열 | profile-modal.js(✅ 저장 동작, 백엔드에서는 organization_id·profile 필드로 이관) |
| `avatar_<email>` | base64 dataURL | profile-modal.js |
| `ef_inquiries` | 배열, 각 항목에 `answeredBy`(문자열, 이메일)/`answeredAt`(문자열, YYYY.MM.DD) 포함 | assets/js/inquiries.js (✅ 2026-08-07 답변자/답변일 자동 기록 추가) |
| `ef_mentoring` | 배열 `[{id, email, mentorEmail, mentorName, category, title, status, messages[]}]` | assets/js/mentoring.js (✅ 2026-08-06 "쪽지 1건-답변 1건"→대화 스레드로 재구성, `content`/`answer` 필드는 `messages[]`로 대체됨) |
| `ef_checklist_progress_<org>` | 중첩 객체(period→sector→category→item) | 인생도서관_업무체크리스트.html |
| `ef_notices` | 배열 | 안내센터_공지사항.html, _상세.html |
| `ef_home_widgets_<email>` | 배열 `[{id, size}]` | index.html |
| `ef_mail_data` | 배열 | 업무배송_업무발송.html, _업무우편함.html |
| `ef_mailSubscriptions_<user>` | 배열 | 업무배송_업무우편함.html |
| `ef_mailCalendarBookmarks_<user>` | 배열 | 업무배송_업무우편함.html |
| `ef_events`, `ef_done`, `ef_settings`, `ef_school`, `ef_depts`, `ef_ai_cache`, `ef_sideopen` | 각각 다름 | 업무배송_스마트공문달력.html |
| `ef_saved_resources` | 배열 | 인생도서관_마이페이지.html |
| `ef_chat_history_<email>` | 배열 `[{id, title, date, html}]` | AI업무어시스턴트_챗봇.html (서버 이전 시 HTML 대신 구조화 메시지 저장) |
| `ef_mentor_slots` | 배열 `[{id, mentorEmail, date, time, booked}]` | assets/js/mentoring.js (`EF_APPOINTMENT`, ✅ 2026-08-06 신규, 멘토별 분리) |
| `ef_appointments` | 배열 `[{id, slotId, requestId, mentorEmail, menteeEmail, platform, joinLink, meetingId, meetingPassword, ...}]` | assets/js/mentoring.js (`EF_APPOINTMENT`, ✅ 2026-08-06 신규, `ef_upcoming_appointments`를 대체) |
| `ef_mentor_roster` | 배열 `[{email, name, categories[]}]` | assets/js/mentoring.js (`EF_MENTOR_ROSTER`, ✅ 2026-08-06 신규), 서버관리자_멘토배정관리.html에서 편집 |
| `ef_consult_logs` | 배열 `[{id, mentorEmail, requestId, category, date, summary, action, status}]` | assets/js/mentoring.js (`EF_CONSULT_LOG`, ✅ 2026-08-06 신규) |
| `ef_footer_org`, `ef_footer_address`, `ef_footer_copyright` | 문자열 | 전 페이지 푸터, 서버관리자_푸터관리.html에서 편집 |
| `ef_contact_dept`, `ef_contact_phone`, `ef_contact_email` | 문자열 | 개인정보처리방침 등, 서버관리자_푸터관리.html에서 편집 |
| `ef_dpo_name`, `ef_dpo_title`, `ef_dpo_phone` | 문자열 | 개인정보처리방침, 서버관리자_푸터관리.html에서 편집 |
| `mockUsers` | 배열(시드 5명) | member-management.js 전용, 실데이터 아님 |
| `ef_security_ip_allowlist`, `ef_security_ip_blocklist` | 배열 `[{ip, memo}]`류 | 서버관리자_보안설정.html (✅ 2026-08-07 신규, 프론트에서만 유효 — 실배포 시 서버 보안 정책으로 재구현 필요) |
| `ef_security_mfa_enabled`, `ef_security_pw_expiry_days`, `ef_security_concurrent_limit` | 각각 boolean/숫자 문자열 | 서버관리자_보안설정.html (✅ 2026-08-07 신규, 마찬가지로 실배포 시 서버 정책 필요) |
| `ef_rag_source_status` | 객체 `{sourceId: 'optimal'}` | 서버관리자_RAG데이터관리.html (✅ 2026-08-07 신규, Reconnect 상태 유지용) |
| `ef_rag_school_uploads_<org>` | 배열 `[{name, size, uploadedAt}]` | 학교관리자_RAG데이터갱신.html (✅ 2026-08-07 신규, 파일명/크기만 — 실제 바이너리 스토리지 아님) |

---

## 7. 이 문서의 한계

- 이번 조사는 여러 차례의 정적 점검과 HWPX ZIP/XML 메모리 검증 결과를 종합한 것으로, 매우 구체적인 라인 번호까지
  전부 재검증하지는 않았습니다(특히 섹션 4의 서식자료실/관리자 부분은 원 조사 결과를 요약한 것이므로,
  실제 구현 착수 전 해당 파일을 직접 열어 함수명 기준으로 재확인 권장).
- 콘텐츠 자체의 정책적 정확성(예: 개인정보처리방침의 "고려대학교 공공정책연구소 위탁" 문구 등 실제
  법무 검토가 필요한 텍스트)은 이 문서의 범위 밖입니다.
- AI(챗봇, OCR, 서식자동완성, 계획서변환)의 실제 모델/프롬프트 설계는 다루지 않았습니다 — 이 문서는
  "현재 프론트가 이 기능을 어떤 입출력 형태로 기대하는가"까지만 정리했습니다.
