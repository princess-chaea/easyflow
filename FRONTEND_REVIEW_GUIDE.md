# EasyFlow 프론트 최종 협의회 가이드

작성일: 2026-08-15
목적: 백엔드가 없는 상태에서도 주요 사용자 흐름을 끝까지 검토하되, 데모 결과를 실제 연동으로 오인하지 않게 한다.

## 1. 운영 원칙

API를 제거한다는 말은 **브라우저에 있던 외부 공급자 키와 직접 호출을 제거한다**는 뜻이다. 화면의 사용자 흐름까지 제거하거나 버튼을 막지 않는다.

- 1순위: 동일 출처 백엔드 프록시 호출
- 2순위: 로컬 `file:` 실행 또는 `demoFallback: true` 개발 환경에서 프록시가 없거나 실패하면 결정론적 데모 데이터
- 화면 표시: 데모 학교·학사일정·AI 결과마다 `데모` 또는 `협의용 데모` 표기
- 금지: 브라우저 번들에 NEIS·LLM 키 삽입, 외부 모델 API 직접 호출, 데모 결과를 실데이터처럼 표시
- 운영 전환: `window.EF_CONFIG.demoFallback = false`를 명시한다. HTTP(S)는 설정 누락 시에도 데모가 꺼진다.

권장 개발 설정 예시:

```html
<script>
window.EF_CONFIG = {
  apiBase: '',
  demoFallback: true
};
</script>
```

운영 설정 예시:

```html
<script>
window.EF_CONFIG = {
  apiBase: '',
  demoFallback: false
};
</script>
```

## 2. 협의회에서 확인할 사용자 흐름

### A. 학교 검색

1. 회원가입·프로필 또는 스마트 공문 달력에서 `아천`, `안동`, `구미`를 검색한다.
2. 백엔드가 없으면 결과에 `데모` 배지가 표시되는지 확인한다.
3. 학교를 선택했을 때 학교명·코드·상태 문구가 함께 바뀌는지 확인한다.
4. 실제 API 연결 후 같은 UI에서 배지만 사라지고 실데이터로 바뀌는지 확인한다.

### B. 스마트 공문 달력

1. 페이지를 열고 이번 달 학사일정 KPI를 확인한다.
2. 백엔드가 없으면 `[데모]` 일정 3건과 `데모 학사일정` 상태가 표시되어야 한다.
3. 업무 상세의 AI 탭에서 `기안문 초안`, `신규교사 맞춤 코칭`을 실행한다.
4. `AI 주간 브리핑`을 실행한다.
5. 새 일정 모달에서 업무 쪽지를 붙여넣고, 추출 미리보기 → 선택 → 등록까지 진행한다.
6. 모든 데모 결과가 실제 결과와 시각적으로 구분되는지 확인한다.

### C. 교사용 계획서 다듬기

`서식자료실_스마트 계획서 변환.html`에서 다음 순서로 누른다.

1. `예시 계획서 열기`
2. 바꾸고 싶은 내용을 교사 말투로 확인·수정
3. `바꿀 내용 찾기`
4. 바꿀 곳을 체크하고 `선택한 내용 확인하기`
5. 초록색 제안 문장을 직접 수정
6. 각 항목에서 `이대로 반영` 또는 `원래대로` 선택
7. `결과 미리보기`
8. 최종 제목·일시·주요 내용이 선택과 직접 수정값대로 표시되는지 확인
9. `실제 한글 파일 열기`로 다음 편집 영역에 이동

이 흐름은 inlineAI 공개 학습·제품 화면에서 확인되는 **실제 파일 열기 → 지시 → 결과 다듬기**, **선택 영역 전달 → 변경 비교 → 수락/거절** 구조를 참고했다.

- 학습 센터: https://www.inline-ai.com/blog/
- 한/글 기능: https://www.inline-ai.com/feature/hwp/

교사 화면에는 JSON, 패치, 리비전, 영수증, I/O 같은 개발 용어와 다운로드 버튼을 노출하지 않는다.
내부 계약 객체는 백엔드 연결을 위해 코드에 유지하지만 사용자 흐름에서는 숨긴다. 예시 흐름은 실제 파일을
자동 수정하지 않으며, 실제 파일 편집은 다음 절의 브라우저 편집기에서 수행한다.

### D. 실제 HWP/HWPX 웹 편집

1. 같은 페이지의 `한글 파일을 웹에서 바로 편집하기`에서 HWP/HWPX 사본을 선택한다.
2. 문서 페이지 수와 자체 호스팅 편집 화면이 나타나는지 확인한다.
3. 본문을 직접 수정한다.
4. `HWP 수정본 저장`과 `HWPX 수정본 저장`을 각각 확인한다.
5. 내려받는 이름이 `원본명_수정본.hwp[x]`인지, 원본은 덮어쓰지 않는지 확인한다.
6. 잘못된 확장자, 빈 파일, 50MB 초과, 확장자와 실제 형식 불일치가 교사 언어로 안내되는지 확인한다.

편집기는 `rhwp` 정적 배포본과 `@rhwp/editor 0.8.2`를 웹앱에 포함해 동일 출처에서 실행한다. 선택한
문서 바이트를 제3자 편집 서버로 보내지 않는다. 반입 버전·해시·라이선스는 `THIRD_PARTY_NOTICES.md`를 따른다.

## 3. 기능 상태 판정표

| 기능 | 협의회에서 보이는 것 | 현재 실제성 | 백엔드·로컬 구현 후 교체점 |
|---|---|---|---|
| 학교 검색 | 검색·선택 전체 흐름 | 프록시 우선, 실패 시 데모 | `GET /api/neis/schools` |
| 학사일정 | 월별 일정·KPI | 프록시 우선, 실패 시 데모 | `GET /api/neis/schedules` |
| 기안·코칭·브리핑 | 생성·캐시·복사 | 프록시 우선, 실패 시 고정 데모 | `POST /api/ai/generate` |
| 쪽지 일정 추출 | 붙여넣기·미리보기·등록 | 프록시 우선, 실패 시 제한적 데모 파싱 | `POST /api/ai/generate` |
| 체크리스트 HWPX | 실제 파일 생성 | 내장 HWPX 치환 실동작 | 서버 불필요, 템플릿 회귀검사 유지 |
| 계획서 예시 다듬기 | 요청·항목 선택·문장 직접 수정·결과 미리보기 | 교사용 프론트 상호작용 실동작 | AI 제안은 `POST /api/document-agent/plan`으로 교체 |
| HWP/HWPX 웹 편집 | 실제 파일 열기·편집·두 형식 수정본 저장 | 자체 호스팅 `rhwp` 브라우저 처리 실동작 | 코퍼스 회귀검사·버전 고정·운영 보안헤더 추가 |
| 열린 HWP/HWPX 수정 | 감지·계획 선택·근거/위험·계약 JSON·비교·수락/거절·영수증 | 계약 검증용 상호작용 데모, 실제 문서 I/O 없음 | Windows 로컬 에이전트 + 한컴 Automation |
| 열린 ODT 수정 | 지원 방향 안내 | 미구현 | LibreOffice UNO 우선 PoC |

## 4. 열린 문서 제품 흐름 확정안

1. 활성 문서와 선택 영역 감지
2. 문서 버전·해시와 지원 형식 확인
3. 사용자 지시 수신
4. 수정 계획 생성 — 아직 원문 미변경
5. 사용자가 적용 범위 승인
6. 백업 생성 후 임시 변경 적용
7. 변경 전후 비교
8. 변경별 수락·거절
9. 충돌 여부 재확인
10. 원본 덮어쓰기 또는 새 사본 저장
11. 한/글·LibreOffice에서 재열기 검증 및 감사 로그 기록

## 5. 운영 전 필수 점검

- `demoFallback`이 운영에서 `false`인지 확인
- 데모 학교 코드(`DEMO-`)와 `[데모]` 일정이 운영 네트워크 응답·DB에 저장되지 않는지 확인
- API 실패가 데모로 조용히 위장되지 않고 운영에서는 오류·재시도 UI로 전환되는지 확인
- 외부 공급자 키가 HTML·JS·소스맵·네트워크 응답에 없는지 확인
- AI 결과에 초안·출처·검토 필요 표시가 있는지 확인
- 열린 문서 수정 전 백업, 사용자 승인, 충돌 검사, 감사 로그가 강제되는지 확인
- HWP/HWPX/ODT 저장 후 실제 앱에서 재열기 회귀시험 수행

## 6. 내일 협의회 10분 시연 순서

1. **1분 — 데모/실연동 경계**: `demoFallback` 배지와 운영 전환 원칙을 먼저 설명한다.
2. **2분 — 학교·일정 흐름**: 학교 검색 → 학사일정 → 일정 상세를 보여준다.
3. **2분 — AI 흐름**: 기안문 초안 또는 주간 브리핑을 실행하고 서버 프록시 교체점을 짚는다.
4. **4분 — 핵심 문서 흐름**: C 절의 열린 한/글 문서 수정 9단계를 끝까지 진행한다. 계획에서 `주요 내용`을 제외한 뒤 연도는 수락, 일시는 거절해 선택·제외·결정이 영수증에 분리되는 것을 보여준다.
5. **1분 — 결론**: 프론트 기능 계약은 확정됐고 실제 문서 I/O·검증은 로컬 에이전트/백엔드 책임임을 확인한다.

시연 중 “완료”라고 표현해도 되는 것은 UI 흐름, 선택 상태, JSON 계약, 데모 영수증 생성이다. 실제 문서 수정,
백업, 한/글 재열기, 패키지 무결성 검사는 아직 완료라고 표현하지 않는다.

## 7. 2026-08-15 최종 검증 결과

- 정적 HTML 39개 전부 실제 로그인 세션으로 브라우저 로드
- 로그인 이탈 0건, 빈 화면 0건, 탐색 오류 0건, JavaScript 런타임 예외 0건
- 중복 DOM `id` 0건, 실제 깨진 상대경로 0건
- 열린 문서 데모에서 선택 2/3건 → 1건 수락·1건 거절·1건 제외 → 검토 대기 0건 → 영수증 생성 확인
- 영수증 스키마 `easyflow.document-apply-receipt/v1`, `demo_only`, `documentChanged: false`, 미수행 검사 표기 확인

이번 최종 검사에서 추가로 바로잡은 프론트 오류는 회원가입의 학교 검색 스크립트 중복 로드, 검색 UI가 제거된
시스템 안내 페이지의 null 이벤트 연결, 업무 체크리스트 HWPX 결과 기호의 깨진 JavaScript 문자열이다.

백엔드 담당자는 다음 순서로 읽는다.

1. `BACKEND_HANDOFF.md` — 전체 현황·우선순위·페이지별 교체점
2. `BACKEND_API_CONTRACT.md` — API 요청/응답·오류·업로드·문서 에이전트 영수증
3. `DOCUMENT_AGENT_SPEC.md` — 열린 문서 아키텍처·안전 게이트·GitHub 후보·라이선스·엔진 라우팅
4. 이 문서 — 협의회에서 합의된 UX와 데모/실연동 경계

## 8. 협의회 결정이 필요한 항목

- 기본 진입을 `열린 문서 바로 수정`으로 할지, 업로드형을 병행 노출할지
- 문서 수정 기본값을 `새 사본 저장`으로 고정할지
- 계획 승인 단위를 문단·표 셀·문서 전체 중 어디까지 제공할지
- HWP/HWPX 1차 지원 한컴 버전 범위
- ODT 기준 편집기를 LibreOffice로 고정할지
- 데모 모드 배너를 모든 페이지 공통으로 확장할지
