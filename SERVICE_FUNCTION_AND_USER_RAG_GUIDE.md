# EasyFlow 통합 서비스 기능 및 사용자 참여형 RAG 운영안

작성일: 2026-08-18
문서 성격: 협의·검토용 기능 설명서 / 백엔드 구현 기준 보완안

## 1. 먼저 확인할 결론

질문하신 **“운영자가 준비한 자료뿐 아니라 사용자가 추가한 자료도, 사용자 선택과 관리자 승인을 거쳐 RAG에 반영하는 과정”은 기존 설계에 일부만 반영되어 있었고 완결된 상태는 아니었습니다.**

기존에 반영된 부분은 다음과 같습니다.

- 학교관리자와 서버관리자가 자료를 올려 RAG 데이터로 갱신하는 화면이 있습니다.
- 멘토 상담은 멘토가 `AI 지식 개선 활용`을 명시적으로 선택한 경우에만 개인정보를 제거한 요약을 검토 후보로 보내도록 되어 있습니다.
- 일반 사용자의 RAG 자료 진입점은 `서식 자료실 → 스마트 계획서 → 참고자료·RAG 후보`로 두었습니다. 인생 도서관에 별도 마이페이지가 있다는 전제를 사용하지 않습니다.

기존에 빠져 있어 이번 프론트 보완과 백엔드 계약에 추가한 부분은 다음과 같습니다.

- 일반 사용자가 자신이 올린 자료별로 `AI 참고자료 반영 검토 요청`을 선택하는 기능
- 업무 처리용 파일 업로드와 RAG 제공 동의를 분리하는 원칙
- 학교관리자의 승인·보완 요청·반려 화면과 상태값
- 승인 전에 자동으로 벡터화되지 않도록 막는 서버 규칙
- 승인 범위가 `나만 / 우리 학교 / 교육지원청·교육청 전체` 중 어디까지인지 관리하는 규칙
- 사용자가 요청을 철회했을 때 검색 차단과 벡터 삭제까지 이어지는 절차
- 승인 자료의 출처, 버전, 유효기간, 인용 근거와 감사 로그

따라서 현재 프론트의 `업로드 및 벡터 변환 완료` 연출은 목표 운영 방식과 맞지 않습니다. 실제 서비스에서는 아래와 같이 **선택 → 검토 → 승인 → 색인 → 활용** 순서로 바뀌어야 합니다.

> 이 문서 작성과 함께 해당 요구사항을 `BACKEND_HANDOFF.md`와 `BACKEND_API_CONTRACT.md`에도 구현 기준으로 추가했습니다.

## 2. 여기서 말하는 “RAG 학습”의 정확한 의미

사용자에게는 편의상 “AI 학습에 반영”이라고 안내할 수 있지만, 기술적으로는 AI 모델 자체를 다시 훈련시키는 것이 아닙니다.

- 승인된 문서를 읽을 수 있는 형태로 변환합니다.
- 문서를 작은 단위로 나누고 검색용 임베딩을 만듭니다.
- 질문이 들어오면 권한 범위 안의 관련 문단을 찾습니다.
- 찾은 근거를 AI 답변 생성 시 참고자료로 전달합니다.
- 답변에는 가능하면 자료명·페이지·문단 등 출처를 표시합니다.

즉, **원본 모델의 기억에 영구적으로 넣는 방식이 아니라 승인된 지식 저장소를 검색해 답변 근거로 사용하는 방식**입니다. 화면 문구도 오해가 적은 `AI 참고자료로 반영`, `지식 검색에 반영`을 우선 사용합니다.

## 3. 사용자 자료가 RAG에 반영되는 전체 과정

```text
사용자 자료 업로드
  ↓ 기본값: 개인 자료, RAG 미반영
사용자가 자료별로 “AI 참고자료 반영 검토 요청” 선택
  ↓ 이용 범위·권리·개인정보 확인
보안 검사 + 문서 변환/OCR + 개인정보·민감정보 탐지
  ↓
학교관리자 검토
  ├─ 승인: 우리 학교 범위
  ├─ 보완 요청: 수정 후 재신청
  └─ 반려: 사유 기록, RAG 미반영
  ↓ 더 넓은 공유 범위를 요청한 경우
서버관리자 2차 검토
  ├─ 교육지원청·교육청 전체 범위 승인
  └─ 학교 범위 유지 또는 반려
  ↓
문서 분할 → 임베딩 → 색인 → 품질 확인
  ↓
AI 상담·AI 문서 탐색·스마트 계획서에서 권한에 맞게 검색
  ↓
출처 표시 + 이용 기록 + 갱신·만료·철회 관리
```

### 3.1 업로드와 RAG 제공은 반드시 분리

사용자가 다음 기능에서 파일을 올렸다고 해서 RAG에 자동 반영하면 안 됩니다.

- AI 상담 첨부파일
- AI 문서 탐색의 분석 대상 파일
- 스마트 계획서에서 연 파일
- 지출품의서의 견적서
- 업무 우편함·업무 발송 첨부파일
- 멘토 상담 첨부파일

이 파일들은 기본적으로 **해당 업무를 처리하기 위한 입력**일 뿐입니다. RAG 반영은 사용자가 자료별로 별도 선택하고 설명에 동의한 경우에만 시작합니다. 선택란은 기본 해제 상태여야 하며, 한 번의 동의로 이후 모든 파일을 자동 제공하게 만들지 않습니다.

스마트 계획서의 `참고자료·RAG 후보`는 작업 중 연 파일과 사용자가 추가한 참고자료를 개인 자료로 모아 보여주는 전용 진입점입니다. 동일 해시 파일은 다시 저장하지 않고, 제목·연도·버전이 비슷한 파일은 하나의 문서 계열로 묶습니다. 사용자가 묶음별로 검토를 요청해야 학교관리자 승인함에 나타납니다.

### 3.2 사용자가 선택할 내용

사용자는 자료별로 다음 내용을 확인하고 검토를 요청합니다.

- 자료 제목과 설명
- 자료 종류와 업무 분야
- 적용 학교급: 기본 `공통 / 유·초등 / 중·고등` 복수 선택. 특정 학교급 전용 자료만 세부 학교급 사용
- 기준연도와 같은 문서 계열을 연결하는 식별자
- 본인이 올릴 권한이 있는 자료인지 확인
- 학생·학부모·교직원 개인정보가 포함되지 않았는지 확인
- 희망 공유 범위: `우리 학교` 또는 `교육지원청·교육청 전체 검토 요청`
- 유효기간 또는 기준 연도
- 이전 문서를 대체하는 자료인지 여부
- 언제든 검토 요청 또는 활용 동의를 철회할 수 있다는 안내

`나만 사용`은 RAG 제공이 아니라 개인 문서 검색 기능입니다. 이 경우에도 개인 전용 색인을 만들 수는 있지만 다른 사용자에게 검색되어서는 안 됩니다.

업무 분류는 학교급별로 나눕니다. 유치원·초등학교는 유·초등 업무 30개, 중학교·고등학교는 중·고등
업무 29개를 사용합니다. 중·고등 목록에는 `교육과정`이 있고 `늘봄`, `문화예술`은 없습니다. `공통`은
행정실 업무·교원인사처럼 모든 학교급에서 함께 참고할 자료 범위이며 별도 학교 종류로 취급하지 않습니다.

### 3.3 관리자 검토 기준

관리자는 원문과 자동 검사 결과를 보고 다음을 확인합니다.

- 주민등록번호, 연락처, 건강·상담·징계·인사 정보 등 개인정보·민감정보 포함 여부
- 비밀번호, API 키, 내부망 주소 등 보안정보 포함 여부
- 저작권·배포권·내부 공유 권한 확보 여부
- 시행 중인 최신 규정인지, 폐기되거나 오래된 양식인지 여부
- 제목·본문·첨부 내용의 일치 여부
- 중복 문서, 악성 파일, 품질이 낮은 OCR 결과 여부
- 학교 전용인지 더 넓은 범위에서 활용 가능한지 여부
- 답변 근거로 인용할 수 있는 페이지·문단 정보가 생성되는지 여부

검토 결과는 `승인`, `보완 요청`, `반려` 중 하나이며 사유를 사용자에게 보여줍니다. 관리자가 파일을 직접 고쳐 승인하는 방식보다는, 필요한 수정사항을 알려 사용자가 다시 제출하도록 하는 편이 책임과 버전 이력을 명확하게 합니다.

### 3.4 승인 범위

| 승인 범위 | 승인자 | 검색 가능한 사용자 | 예시 |
|---|---|---|---|
| 개인 | 별도 관리자 승인 없음 | 업로드한 본인 | 개인 업무 메모, 개인 초안 |
| 우리 학교 | 해당 학교관리자 | 같은 학교의 권한 있는 사용자 | 교내 업무 절차, 학교 자체 서식 |
| 교육지원청·교육청 전체 | 학교관리자 확인 후 서버관리자 | 승인된 관할 조직 사용자 | 일반화 가능한 우수 사례, 공통 업무 가이드 |
| 시스템 공식 자료 | 서버관리자 | 정책에 따른 전체 사용자 | 교육청 공식 지침, 검증된 공통 서식 |

학교관리자는 다른 학교 자료를 승인하거나 열람할 수 없습니다. 서버관리자도 업무상 필요한 최소 권한과 감사 로그를 전제로 접근합니다.

### 3.5 상태 흐름

| 상태 | 사용자에게 보이는 의미 | RAG 검색 사용 |
|---|---|---|
| `PRIVATE` | 개인 자료로 저장됨 | 본인 전용 기능 외 사용 안 함 |
| `SUBMITTED` | 검토 요청 접수 | 사용 안 함 |
| `UNDER_REVIEW` | 관리자가 검토 중 | 사용 안 함 |
| `CHANGES_REQUESTED` | 개인정보 제거 등 보완 필요 | 사용 안 함 |
| `REJECTED` | 반려됨, 사유 확인 가능 | 사용 안 함 |
| `APPROVED` | 승인되어 색인 대기 | 아직 사용 안 함 |
| `INDEXING` | 문서 변환·벡터화 중 | 아직 사용 안 함 |
| `ACTIVE` | AI 참고자료로 사용 중 | 승인 범위에서 사용 |
| `SUSPENDED` | 오류·정책 변경으로 일시 중지 | 사용 안 함 |
| `WITHDRAWN` | 사용자 철회 또는 관리자 폐기 | 즉시 검색 차단 후 벡터 삭제 |

현재 학교관리자 화면처럼 업로드 직후 `벡터화 완료`로 표시하면 승인 절차를 건너뛰게 됩니다. 업로드 직후에는 `검토 대기`가 맞고, 실제 색인은 `APPROVED` 이후 비동기 작업으로 실행해야 합니다.

### 3.6 철회·갱신·삭제

- 사용자는 검토 중인 요청을 취소할 수 있습니다.
- 이미 활성화된 자료의 활용 동의를 철회하면 먼저 검색 대상에서 즉시 제외합니다.
- 이후 원문 보존정책에 따라 파일을 삭제하고, 해당 버전의 청크와 임베딩도 삭제합니다.
- 새 버전이 등록되면 기존 승인을 자동 승계하지 않고 다시 검토합니다.
- 만료되거나 폐기된 규정은 `SUSPENDED` 처리하고 새 답변 근거로 사용하지 않습니다.
- 어떤 답변에 어떤 자료가 인용되었는지는 자료 ID와 버전 기준으로 추적합니다.

상담 대화는 일반 문서보다 더 엄격하게 다룹니다. 현재 원칙처럼 원문을 직접 RAG에 넣지 않고, 명시적 선택이 있는 경우에만 개인정보를 제거한 요약을 별도 검토 대상으로 만듭니다.

## 4. 사용자·관리자 화면에 필요한 변화

### 4.1 일반 사용자

`서식 자료실 → 스마트 계획서 → 참고자료·RAG 후보`를 사용자 자료 관리와 검토 요청의 단일 진입점으로 사용합니다.

- 작업 중 연 파일과 추가 참고자료의 개인 목록·삭제
- 동일 해시 중복 제거와 유사 제목·연도·버전 묶음
- `개인 자료 묶음`과 `RAG 검토 요청 자료 묶음` 구분
- 묶음별 학교급·기준연도·업무분야·공유 범위 입력과 동의 설명
- 검토 상태, 관리자 의견, 승인 범위 표시
- 보완 후 재신청
- 검토 요청 취소와 활성 자료 철회
- 해당 자료가 AI 답변에 사용된 횟수와 최근 인용일은 개인정보가 드러나지 않는 집계로 선택 제공

AI 상담·문서 탐색 등 다른 메뉴에서 올린 파일은 자동 등록하지 않습니다. 향후 사용자가 명시적으로
`스마트 계획서 참고자료로 보내기`를 선택한 경우에만 같은 개인 자료 묶음으로 연결할 수 있습니다.

### 4.2 학교관리자

`RAG 데이터 갱신` 메뉴를 `RAG 자료 검토·갱신` 역할로 확장합니다.

- 우리 학교 사용자가 신청한 자료의 승인 대기함
- 원문 미리보기와 개인정보·중복·악성코드·OCR 품질 검사 결과
- 승인 범위와 업무 분야 지정
- 승인, 보완 요청, 반려와 사유 입력
- 승인된 학교 자료의 활성·중지·교체·만료 관리
- 서버관리자에게 광역 공유 승격 요청
- 학교별 승인·반려·철회·검색 활용 현황

학교관리자가 직접 올린 자료도 자체 업로드 즉시 활성화하지 않고, 최소한 업로더와 승인자를 분리하는 2인 검토를 권장합니다. 소규모 운영에서 예외를 허용하더라도 누가 승인했는지 감사 로그는 반드시 남깁니다.

### 4.3 서버관리자

`RAG 데이터 관리`에 다음 기능을 추가합니다.

- 학교별 승인 자료와 광역 공유 승격 요청 조회
- 교육지원청·교육청 전체 범위의 2차 승인
- 공식 자료와 사용자 기여 자료를 출처 유형으로 구분
- 데이터 소스별 버전·색인 상태·오류·문서 수·청크 수 관리
- 검색 결과 품질, 근거 없는 답변, 오래된 자료 탐지
- 자료 중지·재색인·폐기와 철회 처리 확인
- 승인자·변경자·검색 범위 변경에 대한 감사 로그

## 5. 프론트엔드와 백엔드가 결합된 뒤 메뉴별 완성 기능

아래는 현재 메뉴 구성을 기준으로, 백엔드가 연결되었을 때 각 메뉴가 담당할 최종 역할입니다.

### 5.1 홈

| 메뉴 | 완성 후 가능한 기능 |
|---|---|
| 홈 대시보드 | 사용자가 고른 위젯의 종류·순서·크기를 계정별로 저장합니다. 내 업무 마감, 새 업무 우편, 상담 현황, 체크리스트 진행률, 최근 공지, 추천 서식을 실제 데이터로 요약합니다. 통합 검색은 권한이 있는 공문·자료·서식·상담·공지 범위에서 결과를 제공합니다. |

### 5.2 업무 배송

| 메뉴 | 완성 후 가능한 기능 |
|---|---|
| 스마트 공문 달력 | 교육청 업무 일정, NEIS 학사일정, 개인 일정, 업무 우편함 책갈피를 한 달력에서 봅니다. 완료 여부와 메모를 계정별로 저장하고 Google·Outlook의 별도 EasyFlow 캘린더와 동기화합니다. 선택한 일정은 AI가 기안 초안·준비사항·주간 브리핑으로 정리합니다. |
| 업무 우편함 | 유치원·초등학교는 유·초등 업무 분류, 중학교·고등학교는 중·고등 업무 분류로 맞춤 수신합니다. 읽음, 책갈피, 첨부파일, 마감일, 변경·회수 알림을 관리하며 책갈피한 업무를 달력으로 연결합니다. |
| 업무 발송 | 권한이 있는 업무담당자가 자신의 조직 학교급에 맞는 업무 분야, 대상 학교·부서, 발송·마감 시기, 본문, 파일·링크를 등록합니다. 예약 발송, 수정, 회수, 수신·확인 현황, 버전과 감사 이력을 관리합니다. |
| 맞춤형 업무 가이드 | 로그인 사용자의 소속·역할·부서, 수신 업무, 학사일정, 미완료 체크리스트를 종합해 오늘과 이번 주의 우선 업무를 추천합니다. 필요한 지침·서식·선행 업무로 바로 연결합니다. |

### 5.3 인생 도서관

| 메뉴 | 완성 후 가능한 기능 |
|---|---|
| 업무 체크리스트 | 학교 구성원이 같은 점검표를 공동 사용합니다. 이행·미흡·미이행, 조치사항, 점검자, 점검일을 서버에 저장하고 기간·분야별 결과와 HWPX 보고서를 만듭니다. 미흡 항목은 관련 지침·서식과 학교관리자 RAG 갱신 과제로 연결합니다. |
| AI 상담(챗봇) | 질문과 첨부파일을 분석하고, 사용자 권한 안에서 승인된 RAG 자료를 검색해 근거가 있는 답변을 제공합니다. 출처와 기준일을 표시하며 답변이 부족하면 담당 멘토 상담으로 이어집니다. |
| 멘토 찾기·신청 | 업무 분야별 담당 멘토, 상담 가능 시간, 진행 중·완료 상담, 화상 상담 예약을 관리합니다. 자료 기여와 RAG 검토 상태 관리는 이 메뉴가 아니라 스마트 계획서에서 처리합니다. |
| 멘토 상담함 | 멘토가 배정된 상담 스레드에 답변하고 파일을 교환하며 상담 시간을 개설하고 상담일지를 작성합니다. 상담 종료 후 명시적으로 선택한 경우에만 익명화 요약을 RAG 검토 후보로 보냅니다. |
| 인생 도서관 기록 | 장학사·업무담당자가 권한 범위 안의 상담 분야, 처리기간, 반복 질문, 미해결 과제를 집계합니다. 개인 대화 원문을 무단 열람하지 않고 익명·집계 지표로 정책과 지원 필요 영역을 찾습니다. |

### 5.4 서식 자료실

| 메뉴 | 완성 후 가능한 기능 |
|---|---|
| AI 문서 탐색 | 공문 캡처·PDF·HWP/HWPX·계획서 등을 분석해 해야 할 업무를 추출하고, 사용자의 학교급 자료와 공통 승인 자료 중 관련 서식·계획서를 추천합니다. 추천 이유와 원문 페이지·관련 문장을 함께 보여줍니다. |
| 스마트 계획서 | 브라우저에서 HWP/HWPX를 열고 편집합니다. 작업 중 연 문서와 추가 참고자료를 개인 자료로 모으고, 완전 중복은 해시로 제거하며 유사 제목·연도·버전은 대표본 중심으로 묶습니다. 사용자는 묶음별 학교급·기준연도·업무분야·공유 범위를 지정해 RAG 검토를 요청할 수 있습니다. 대화형 AI는 승인된 RAG 근거와 문서 문맥을 바탕으로 수정안을 제안하고, 사용자가 수락한 변경만 반영합니다. |
| 지출품의서 작성기 | CSV·PDF·이미지·HWP/HWPX·엑셀 견적서에서 업체와 품목·규격·수량·단가를 추출합니다. 합계와 오류를 검토하고 품의 제목·본문 및 K-에듀파인용 CSV를 생성합니다. 예시 자료의 업체명이나 금액을 실제 결과에 섞지 않습니다. |

과거의 `서식 자동 완성` 단계형 메뉴는 정식 메뉴에서 제외되어 있으며, 가치 있는 기능은 스마트 계획서로 통합합니다.

### 5.5 안내센터와 계정

| 메뉴 | 완성 후 가능한 기능 |
|---|---|
| 공지사항 | 운영자가 작성한 공지를 대상 역할·학교에 게시하고 조회수, 첨부파일, 게시기간을 관리합니다. |
| 이용 안내·1:1 문의 | 사용자 매뉴얼을 제공하고 문의를 접수합니다. 사용자는 자신의 문의와 답변을 보고, 담당 관리자는 권한 범위의 문의를 배정·답변·종결합니다. |
| 로그인·회원가입·프로필 | 실제 사용자 인증, 학교 검색, 소속·부서·직위, 아바타, 추가 역할 신청을 관리합니다. 역할별 메뉴와 데이터 접근 권한은 서버에서 판정합니다. |
| 법적 고지 | 개인정보처리방침, 이용약관, 이메일 수집 거부, 기관 위치와 담당자 정보를 중앙 설정에서 일관되게 제공합니다. |

### 5.6 학교 관리

| 메뉴 | 완성 후 가능한 기능 |
|---|---|
| 대시보드 | 우리 학교의 수신·처리·완료 업무, 예정 일정, 체크리스트, 상담·AI 활용 지표를 실제 데이터로 요약합니다. |
| 학교 통합 관리 | 부서별 업무량과 담당 현황, 지연·미배정 업무를 조회하고 권한 있는 관리자가 재배치합니다. 변경은 실제 업무 배정과 감사 로그에 반영합니다. |
| 데이터 리포트 | 기간·부서·업무 분야별 처리시간, 체크리스트 이행률, AI 활용과 지원 필요 영역을 분석해 CSV·인쇄용 리포트로 내보냅니다. |
| RAG 자료 검토·갱신 | 학교관리자 직접 자료와 일반 사용자의 검토 요청을 함께 관리합니다. 승인·보완·반려, 학교 범위 활성화, 광역 공유 승격 요청, 중지·교체·만료를 처리합니다. |

### 5.7 서버 관리

| 메뉴 | 완성 후 가능한 기능 |
|---|---|
| 대시보드 | 사용자·역할 승인, 미답변 문의, 진행 중 상담, RAG 작업 오류, 보안 경고와 인프라 상태를 실제 운영 지표로 보여줍니다. |
| 리소스 모니터링 | API·DB·파일 저장소·문서 변환·OCR·LLM·벡터 검색의 CPU, 메모리, 지연시간, 오류율, 큐 적체와 비용을 감시합니다. |
| 보안 설정 | 관리자 MFA, IP 허용·차단, 비밀번호·세션 정책을 실제 서버 정책으로 적용하고 보안 이벤트와 변경 이력을 관리합니다. |
| 회원 관리 | 사용자 검색, 계정 상태, 소속, 기본 역할과 추가 역할, 정지·복구를 관리합니다. |
| 역할 관리 | 사용자가 신청한 역할을 범위와 유효기간에 따라 승인·반려하고 메뉴·API 접근권한에 반영합니다. |
| RAG 데이터 관리 | 공식 자료, 학교 자료, 사용자 기여 자료의 검토·승격·색인·버전·품질·철회·삭제를 통합 관리합니다. |
| 푸터 관리 | 기관명, 주소, 대표전화, 저작권, 개인정보 담당부서와 보호책임자 정보를 전체 사이트에 반영합니다. |
| 멘토 배정 관리 | 업무 분야별 담당 멘토를 복수 배정하고 미배정 분야를 확인해 상담 신청을 자동 라우팅합니다. |

## 6. 메뉴들이 서로 연결되어 만드는 주요 서비스 흐름

### 6.1 업무 수신에서 문서 작성까지

1. 업무담당자가 `업무 발송`에서 대상과 마감을 지정합니다.
2. 대상 사용자는 `업무 우편함`에서 받고, 책갈피한 업무는 `스마트 공문 달력`에 표시됩니다.
3. `맞춤형 업무 가이드`가 마감과 선행 준비를 우선순위로 정리합니다.
4. 사용자는 `AI 문서 탐색`에서 관련 지침·서식을 찾습니다.
5. `스마트 계획서` 또는 `지출품의서 작성기`에서 실제 문서를 완성합니다.
6. 완료 상태와 이력은 홈·학교관리자 대시보드·리포트에 집계됩니다.

### 6.2 AI 상담에서 사람 상담과 지식 개선까지

1. 사용자가 `AI 상담`에서 질문합니다.
2. AI는 승인된 RAG 근거를 검색해 출처와 함께 답합니다.
3. 해결되지 않으면 업무 분야에 맞는 멘토에게 상담을 신청합니다.
4. 멘토가 답변하고 상담일지를 남깁니다.
5. 멘토가 명시적으로 선택한 경우 익명화 상담 요약만 관리자 검토 대상으로 보냅니다.
6. 승인된 요약은 이후 같은 유형의 질문에 참고자료로 사용됩니다.

### 6.3 학교 점검에서 필요한 자료 보강까지

1. 학교 구성원이 `업무 체크리스트`를 공동 점검합니다.
2. 학교관리자는 대시보드와 리포트에서 미흡·미이행 분야를 확인합니다.
3. `RAG 자료 검토·갱신`은 해당 분야에 부족한 지침·우수 사례를 우선 제안합니다.
4. 학교관리자가 공식 자료를 추가하거나 사용자 기여 요청을 검토합니다.
5. 승인된 자료가 AI 상담·문서 탐색·스마트 계획서에 반영됩니다.

## 7. 백엔드에 필요한 최소 데이터 구조

| 데이터 | 역할 |
|---|---|
| `files` / `user_resources` | 원본 파일, 소유자, 조직, 저장 위치, 해시, MIME, 보존기간, HWP 원본과 PDF 파생본 관계 |
| `document_families` | 통합본·분할본·연도별 개정본·초안/최종본을 묶는 문서 계열과 대표본 |
| `rag_submissions` | 사용자 검토 요청, 학교급, 기준연도, 업무분야, 희망 범위, 상태, 동의 시각, 철회 시각 |
| `rag_reviews` | 검토자, 결정, 사유, 승인 범위, 검토 시각 |
| `rag_sources` | 승인되어 지식 원천이 된 자료와 현재 활성 상태 |
| `rag_source_versions` | 버전, 시행일, 만료일, 대체 관계, 원문 해시 |
| `rag_chunks` | 페이지·문단 앵커가 있는 검색 단위와 임베딩 참조 |
| `rag_jobs` | 변환·OCR·개인정보 검사·색인·삭제 작업 상태 |
| `rag_consent_events` | 요청, 범위 변경, 철회 등 사용자 선택 이력 |
| `audit_logs` | 관리자 승인·반려·중지·재색인·삭제 이력 |

승인 정보와 원본 파일, 검색 색인은 분리해 관리합니다. 파일을 삭제했는데 벡터만 남거나, 반대로 승인 상태는 활성인데 검색 색인이 사라지는 불일치를 작업 상태와 정합성 검사로 탐지해야 합니다.

실제 전달용 폴더 구조, 파일명, 메타데이터 CSV, HWP 원본과 PDF 투입본 구분은 `RAG_소스파일/README.md`를 따릅니다. 업무별 분할본의 단순 합본은 보관만 하고 색인에서 제외하며 업무별 PDF만 벡터화합니다.

## 8. API에서 보장해야 할 핵심 규칙

- 일반 파일 업로드 API는 절대로 자동 RAG 등록을 하지 않습니다.
- 별도의 RAG 검토 요청 API가 있어야 합니다.
- 승인 권한과 자료 조직이 일치하는지 서버가 검사합니다.
- `APPROVED` 전에는 색인 작업을 시작할 수 없습니다.
- 사용자와 관리자는 목록에서 동일한 상태와 버전을 봅니다.
- 승인·반려·범위 변경·중지·철회는 모두 감사 로그에 남습니다.
- 철회 시 먼저 검색을 차단하고, 원본·청크·임베딩 삭제 작업 결과를 확인합니다.
- AI 검색은 로그인 사용자의 조직·역할과 자료 승인 범위를 매 요청마다 다시 검사합니다.
- AI 답변은 사용한 자료 ID·버전·페이지·문단 앵커를 근거로 반환합니다.

구체적인 엔드포인트와 요청·응답 기준은 `BACKEND_API_CONTRACT.md`의 `6.1 사용자 참여형 RAG 제출·승인 계약`을 따릅니다.

## 9. 완료 판정 기준

다음 조건을 모두 통과해야 이 기능이 완성된 것으로 봅니다.

- 사용자가 파일을 그냥 업로드했을 때 조직 RAG 검색 결과에 나타나지 않습니다.
- 사용자의 명시적 검토 요청이 없으면 관리자 승인함에도 나타나지 않습니다.
- 제출 후 승인 전까지 청크·임베딩을 만들지 않거나, 사전 처리물을 만들더라도 운영 검색 인덱스와 완전히 격리합니다.
- 학교관리자는 자기 학교 요청만 볼 수 있습니다.
- 우리 학교 승인 자료는 다른 학교 사용자에게 검색되지 않습니다.
- 광역 범위 자료는 서버관리자 승인 후에만 넓은 범위에서 검색됩니다.
- 보완 요청과 반려 사유가 사용자에게 표시되고 재신청할 수 있습니다.
- 승인된 자료는 AI 답변에 출처와 버전이 표시됩니다.
- 사용자 철회 또는 관리자 중지 직후 새 검색에서 제외됩니다.
- 원본 변경 시 기존 승인 버전과 섞이지 않고 재검토됩니다.
- 단순 합본과 업무별 분할본이 동시에 검색 인덱스에 들어가 중복 검색되지 않습니다.
- 학교급·기준연도·업무분야 필터가 모든 청크에 이어지고, 과거본은 최신본과 구분됩니다.
- 개인정보 포함 자료, 악성 파일, 권리 확인이 안 된 자료는 활성화되지 않습니다.
- 모든 선택·검토·승인·반려·철회·삭제 이력을 감사할 수 있습니다.

## 10. 구현 우선순위 제안

1. 스마트 계획서 참고자료 저장, 유사 버전 묶음, RAG 검토 요청과 상태 조회
2. 학교관리자 승인 대기함, 보완·반려·학교 범위 승인
3. 승인 후 문서 변환·OCR·청크·임베딩·검색과 출처 표시
4. 철회·중지·버전 교체·만료·감사 로그
5. 서버관리자의 광역 공유 승격 승인과 품질 대시보드
6. 상담 익명화 요약 검토와 체크리스트 기반 자료 보강 추천

이 순서라면 사용자의 통제와 관리자 책임을 먼저 확보한 뒤 RAG 검색 품질을 확장할 수 있습니다.
